# Live Character Guide — Plan & Backlog

> **What is pending, and in what order.** Order authority: `STATUS.md` **Next step** (re-pinned
> every iteration) picks from this backlog; owner-gated rows are not auto-candidates.
> Scope/acceptance live HERE — one row = one future iteration (or a numbered part of one).
> History of completed work: `worklog.md` · `CHANGELOG.md` · git. This file carries no history.
>
> Every backlog row MUST carry: Task · Scope · Acceptance criteria · Required verification · Owner gate (if any).

---

## ACTIVE TRACK (since iter 131): v1 → v2 architecture migration

> Owner track-switch directive (chat 2026-09-14): the incremental editorial cleanup/research track is
> **superseded**; Fork D 2/3, KI#70 (standalone), Fork D 3/3, KI#72 (as content decision), and `dupes-N`
> cleanup passes are **not auto-candidates**. Old work = input only where a direct migration dependency
> exists. **v1 is frozen as immutable migration source; v2 = editable target.**
> Migration order: `semantic extraction → v2 architecture → v2 build → parity audit → canonical audit →
> reader-path audit → switch`. Reader modes: **Learn → Build → Debug → Reference**.
> Evidence base: `docs/research/migration_foundation_iter131.md` · living registry: `docs/research/migration_map_v2.md`.

| ID | Task | Scope | Acceptance criteria | Required verification | Owner gate |
|----|------|-------|---------------------|----------------------|------------|
| v2-build-appendix-d | **v2 build slice — Appendix D (character_map) — the fourth appendix/Reference slice** (guide order, DEC-24 Q6; the iter-147…160 precedent — spec §7 slice shape, maps §5.8–§5.21): (a) evidence — seed the appendix_character_map matrix rows (6) with F2 presentation dispositions + Registry C §5.22 + the stale-note-refresh step (the ::02 KI#77-b/c stale-ref claims describe the pre-iter-127 state — repaired ed-8/R18; the ::04 R17 verdict executed iter 128 — the note describes the pre-strip state); (b) canon — restructure `docs/canon/appendix_character_map.md` per the F5 micro-template where honestly needed (Editorial Policy 5-point check per edit; section IDs preserved) | `docs/canon/appendix_character_map.md` (**canon-only** — no master file, never runtime-rendered; 6 matrix rows); `docs/research/editorial_matrix.md` + `docs/research/migration_map_v2.md` §5.22 + state docs (STATUS/worklog/CHANGELOG/PLAN). Notes: **KI#77-e / R27 budget-range framing stays owner-gated (ed-5)** — the slice documents, does not re-decide; the appendix's conceptual-only section-ID status (the content_map counting convention — 96 rendering + Part 0 ×2 + Appendix D ×1) documented; no root fallbacks (non-rendering) — zero build-side artifacts expected beyond the timestamp churn (restore per AGENTS.md delivery discipline) | the appendix_character_map matrix rows carry presentation dispositions; sync 97/97; parity ×7; build + validate + validate:master + version:check green; tests green; qa baselines exact (english ≤18, syntax ≤238); `git diff --check` clean; scope = only intended files vs BASE_COMMIT; runtime unchanged (96 sections) | NONE — the appendix slices are the pinned track continuation (STATUS Next-step row 1) |

> v2-build-appendix-c — **COMPLETE (executed iter 160):** the pinned STATUS Next-step row 1 (not owner-gated) executed per map §5.21 (P-C-1..P-C-5): evidence — the matrix appendix_glossary table carries the F2 `presentation` column (**26 rows: all `open`** — the Reference-layer verdict holds; count script-verified; the two DUPLICATE rows carry their compressions *inside* the entries) + §1.2 extended to Parts 1–10 + Appendices A/B/C + **the stale-note family refreshed** (the pre-iter-127 «`§X.X` vague» claims + the two R29 quotes — the canon aligns with §6.3/§4.11) + the L-refs re-derived per §1.1 + the R01/R02 §15 rows marked COMPLETE + §16 re-counted (**18 of 20**; #8's iter-153 status de-staled); canon — **the R01/R02 compression candidates EXECUTED** (the slice's pinned differentiator; the KI#70-wire dependency resolved since iter 158): ::03 the placement ПРАВИЛО → 1 sentence + the §1.4 parenthetical ref; ::05 the full annotated 7-item copy + the R13 3-tier restatement → definition + 7-name index + the fixed §7A.2 pointer — **the registry C-3/C-5 deployed forms** (DEC-17c; 5-point checks documented); master — **two mirror completions** (the linked §1.4 parenthetical + the compressed ::05 paragraph with the `#p7a_core_directives` link — fence #10; the `<ol>`'s #3-li gloss gap died with the list) + **the v7-era `part-resume` disposed REMOVED_WITH_REASON** (the iter-158 «B/C slices co-decide» sub-decision executed at C — the B precedent; 5-point check + grep-verified zero tool deps; A's benign instance = the family's sole survivor, 1 live usage); the P-C-4 observation package deferred (the master CoT Tier-0 enrichment, the intro's coverage claim, the front-matter staleness, the heading-mismatch family, the components.md location note); STATUS KI#70 row deleted per lifecycle (closed iter-158, 2 iterations elapsed); NAV §7 de-staled; root fallback `parts/appendix_glossary.html` regenerated (byte-identical to master, sha-verified; `data/glossary.json` byte-stable); full battery green (lint included — the first appendix slice run with eslint in-sandbox); drift 141→140 (the CORE-DIRECTIVES plain-text entry cleared — JSON pre/post diff).

> v2-build-part-10 — **COMPLETE (executed iter 157, + the owner-called KI#84 canonical-audit reconciliation in the same iteration):** the pinned STATUS Next-step row 1 (not owner-gated) executed per map §5.18 (P10-1..P10-6): evidence — the matrix Part 10 tables carry the F2 `presentation` column (**20 rows: 9 `open` + 11 `collapsible`** — all 11 render inside the pre-existing v7-era F6 wraps, zero new wraps; matrix §1.2 extended to Parts 1–10) + four stale budget-row R18 notes refreshed + the verdict refreshed (the refs repaired iter 127 ed-8/R18); canon — **one typo repair**: the L397 «Выщебленного»→«Выщербленного» (the P9-5(e) observation executed; zero F5 restructure — the Parts 5/6/7B/8 precedent); master — the F6-summary mirror repair; zero mirror gaps (the drift pre-capture: 4 comment-noise + 3 KI#79-boundary entries); **KI#84 EXECUTED per the owner's session directive** (the master §9.6 Walter line DELETED — Elena's values attributed to Walter since iter 38; §9.7 = the sole Part-9 Walter reference (C=85/A=25/E=30); canon `part_09.md` byte-stable; both tool anchors moved together: the sync P3-4c → `P3-4c-del` negative check, count 97 preserved (73+24) — + the diagnostics-parity `WALTER_DELETED` absence guard + the new check 2b §9.7 sole-reference guarantee; KI#84 CLOSED); the P10-4 observation package deferred (the Omnis «Звук»/«Голос» iter-75 KI#51 variance incl.); NAV §7 de-staled; root fallbacks `parts/part_10.html` + `parts/part_09.html` regenerated; full battery green; drift 143 unchanged (expected — JSON pre/post diff).

> v2-build-appendix-b — **COMPLETE (executed iter 159):** the pinned STATUS Next-step row 1 (not owner-gated) executed per map §5.20 (P-B-1..P-B-5): evidence — the matrix appendix_model_table table carries the F2 `presentation` column (**4 rows: all `open`** — the Reference-layer verdict holds; count script-verified) **+ the iter-145 НАБЛЮДЕНИЕ row added** (::03, METADATA — the block post-dated the iter-126 audit as DEC-20's first OBSERVATION application; §1.1 document-order re-derivation; audit total 500→**504** disclosed in the matrix header + the map §5 preamble (the script-examined count surfaced 3 pre-existing undisclosed post-audit additions — iter 139/142/147 — now attributed)) + §1.2 extended to Parts 1–10 + Appendices A/B + the ::01 marker inventory refreshed (canon ×4 + master spans ×5, WIRE-live) + the ::04 L-refs refreshed + the appendices verdict extended; canon — **zero edits** (`appendix_model_table.md` byte-stable — F5-approximate verified; the OBSERVATION anchor intact — sampling check 8 PASS); master — **two mirror completions** (the entire «Ключевые следствия для сборки карточки» block restored with the `#p7a_core_directives` link + the Script Tax API-cell word order — fence #10, the v9-slimming-gap precedent) + **the v7-era `part-resume` disposed REMOVED_WITH_REASON** (the iter-158-recorded «B/C slices co-decide» sub-decision executed at B; the iter-47 P2-7 family precedent; 5-point check + grep-verified zero tool deps; the stale MODEL_NOTE bullet disclosed — it directed readers at markers replaced by this very table; A's benign instance remains recorded, C's slice follows the B precedent); the P-B-4 observation package deferred (the A/C part-resume co-travelers, the heading-mismatch informational family, the front-matter staleness, the part_04 master-side span); NAV §7 de-staled; root fallback `parts/appendix_model_table.html` regenerated; full battery green (sampling PASS — the OBSERVATION framing intact); drift 142→141 (the следствия mirror completion cleared the appendix_model_table plain-text entry — simulation pre-verified with the drift tool's tokenizer, best-Jaccard 0.660; JSON pre/post diff).

> v2-build-appendix-a — **COMPLETE (executed iter 158, + the KI#70 WIRE implementation per DEC-24 Q7 in the same iteration):** the pinned STATUS Next-step row 1 (not owner-gated) executed per map §5.19 (P-app-1..P-app-6): evidence — the matrix appendix_mbti table carries the F2 `presentation` column (**6 rows: all `open`** — the Reference-layer verdict holds; count script-verified) + §1.2 extended to Parts 1–10 + Appendix A + the §14 header note + two stale row notes + the appendices verdict refreshed (the pre-iter-127 vague-refs line corrected; the KI#70 never-loaded dependency resolved); canon — **zero edits** (`appendix_mbti.md` byte-stable — F5-approximate verified); master — **three mirror completions** (the S·P note sentence 2 + the `#p7a_system_prompt` link; the «Цвет (в виджете)» header; the «(поведенческие якоря)» gloss — fence #10) + **the stale v9-era transition disposed** (REMOVED_WITH_REASON, the iter-148 §2.3 precedent; grep-verified zero deps); **KI#70 EXECUTED per DEC-24 Q7** (the lazy-loader WIRE: `manifest.appendices` appended to the load list after `manifest.parts` in guide order A→B→C via the `APPENDIX_GUIDE_ORDER` map + the «Приложения» sidebar-TOC group; runtime-verified headless — 93→96 sections, the `#appendix_mbti` TOC link scrolls, the `mbti-composer` initializes with zero page errors; KI#70 CLOSED); the P-app-4 observation package deferred (the part-resume v7-era family, the manifest-order artifact, the B/C/D stale matrix notes); NAV §7 de-staled; root fallback `assets/lazy-loader.js` + `parts/appendix_mbti.html` regenerated; full battery green; drift 143→142 (the S·P-note mirror completion cleared the appendix_mbti plain-text entry — JSON pre/post diff).

> v2-build-part-9 — **COMPLETE (executed iter 156):** the pinned STATUS Next-step row 1 (not owner-gated) executed per map §5.17 (P9-1..P9-7): evidence — the matrix Part 9 tables carry the F2 `presentation` column (**43 rows: 38 `open` + 3 `collapsible` + 1 `canonical-link` + 1 `—`**; matrix §1.2 extended to Parts 1–9) + three stale row notes + the verdict refreshed (the R18 refs repaired iter 128; the §9.11 dupe disposed iter 125; the fold executed); canon — **the §9.4↔§9.5 fold EXECUTED** (the standing `p9_additional_problems::02` candidate: the #4/#5 table folded into §9.5 — rows relabeled «#4:»/«#5:», §9.4 keeps the pointer form — the only canon content edit; **the §9.6 cluster owner byte-stable, sha-verified**); master — five «Ссылка» mirror completions (incl. the §9.7 mangled v9 fragment replaced) + the fold mirror + the §9.11 worked-example F6 wrap (16→17 `<details>`) with the «Выщебленного» typo repair; **KI#84 opened** (the master §9.6 Walter line value defect — deferred); the sampling-audit check 7 re-pointed in step (disclosed); NAV §7 de-staled; root fallback `parts/part_09.html` regenerated; full battery green; drift 148→143 (five canon-side matches gained — JSON pre/post diff).

> v2-build-part-8 — **COMPLETE (executed iter 155):** the pinned STATUS Next-step row 1 (not owner-gated) executed per map §5.16 (P8-1..P8-5): evidence — the matrix Part 8 tables carry the F2 `presentation` column (**40 rows: all `open`** — the Debug-mode catalog Part; zero qualifying Collapsible blocks; zero `canonical-link` / `—`; matrix §1.2 extended to Parts 1–8) + **eleven stale row notes + the verdict defect-load line refreshed** (the vague-ref/paren defects repaired iter 127 ed-8/R18 — pickaxe `8a365553`; the R17-lite phrase stripped iter 129; the KI#72 conditional superseded by DEC-22); canon — **zero content edits** (`part_08.md` byte-unchanged — sha256 verified; the Part hosts no cluster owner); master — **sixteen mirror completions** (the v9 Phase-6 slimming gaps: the dropped canon clauses / sentences / «Ссылка» rows / links restored — fence #10 canon wins; the §8.1 label re-point «Примечание»→«Ссылка» with the sync P1-3 probe substring updated in step — 2 lines in `scripts/audit_canon_master_sync.py`, disclosed, the iter-148 probe-extension precedent); the master-only enrichments + variances recorded as deferred observations (P8-4 — ride the canonical-audit stage); NAV §7 de-staled; root fallback `parts/part_08.html` regenerated; full battery green; drift 154→148 (six canon-side matches gained — JSON pre/post diff; zero new rows).

> v2-build-part-7b — **COMPLETE (executed iter 154):** the pinned STATUS Next-step row 1 (not owner-gated) executed per map §5.15 (P7B-1..P7B-5): evidence — the matrix Part 7B tables carry the F2 `presentation` column (33 rows: 25 `open` + 8 `collapsible` — all eight pre-existing deployed F6 usages documented; zero `canonical-link` / `—` — the Part verdict «clean; zero compression candidates» held; matrix §1.2 extended to Parts 1–7B) + one stale note refreshed (`p7b_lorebook_advanced::06` — the R29 fix executed iter 129; the note described the pre-fix state) + the E18 VS-shadow + §4.2-target re-verification notes; canon — **zero content edits** (`part_07b.md` byte-unchanged; the Part hosts no cluster owner, no fold candidate); master — **three mirror completions** (the §7B.3::04 ref-target fix — canon `[ref: §7B.4]` vs the v9-era `#p7b_lorebook_advanced` link, pickaxe-verified, fence #10 canon wins; the §7B.3::11 + §7B.4::07 «Ссылка» label/anchor completions — the P5-3/P6-3 families); the master-only enrichments + variances (the E18 canon-declaration gap, the EN-label family sibling, the closing transition) recorded as deferred observations (P7B-4 — ride the canonical-audit stage); NAV §7 de-staled; root fallback `parts/part_07b.html` regenerated; full battery green; drift 155→154 (the §7B.4::07 «Ссылка» paragraph gained its master mirror).

> v2-build-part-7a — **COMPLETE (executed iter 153):** the pinned STATUS Next-step row 1 (not owner-gated) executed per map §5.14 (P7A-1..P7A-5) — the largest slice so far (13 sections / 94 rows, executed whole; the sub-slicing option not needed): evidence — the matrix Part 7A tables carry the F2 `presentation` column (94 rows: 86 `open` + 4 `canonical-link` — 3 already-deployed forms re-verified + the R01 #8 fold — + 2 `collapsible` — both pre-existing F6 usages documented — + 2 `—` historical rows; matrix §1.2 extended to Parts 1–7A + the `—` rule) + seven stale notes refreshed (the KI#71/ed-6/iter-125/iter-128 pre-fix states); canon — **one content edit**: the R01 #8 §7A.11 fold (the Anchors-placement conceptual sentence → the `[ref: §1.4]` pointer, 5-point check passed, the Part-4 R21-#16 precedent; the §7A.2/§7A.6/§7A.12 cluster-owner records byte-stable — the three cluster parity audits PASS); master — the fold mirror (§7A.11 callout, `[ref:]` rendered as the §1.4 anchor); the master-only enrichments + §7A.7 family variances recorded as deferred observations (P7A-4 — ride the canonical-audit stage); NAV §7 de-staled; root fallback `parts/part_07a.html` regenerated; full battery green; drift 155 unchanged.

> v2-build-part-6 — **COMPLETE (executed iter 152):** the pinned STATUS Next-step row 1 (not owner-gated) executed per map §5.13 (P6-1..P6-5): evidence — the matrix Part 6 tables carry the F2 `presentation` column (25 rows, all `open` — Learn-mode core teaching Part for CoT, zero qualifying Collapsible blocks) + one stale note refreshed (`p6_cot_tiers::05` — the R17 strip executed iter 128; the note described the pre-strip state); canon — **zero content edits** (`part_06.md` byte-unchanged; the Tier 0–3 canonical table + the Tier 2/Tier 3 templates byte-stable); master — the §6.3::04 «Ссылка» mirror (the missing Reference-branch block, pickaxe-verified never present; ref-notation rendered reader-facing per the P5-3 precedent); the six master-only enrichments + variances (the E11 canon-declaration gap incl.) recorded as deferred observations (P6-4 — ride the canonical-audit stage); KI#83 row lifecycle-deleted; NAV §7 de-staled; root fallback `parts/part_06.html` regenerated; full battery green; drift 155 unchanged.

> v2-build-part-4 — **COMPLETE (executed iter 150):** the pinned STATUS Next-step row 1 (not owner-gated) executed per map §5.11 (P4-1..P4-6): evidence — the matrix Part 4 tables carry the F2 `presentation` column (51 rows: 50 `open` + 1 `canonical-link`) + two stale notes refreshed (KI#77-a repair iter 127; R29 normalization iter 129) + R21/candidate #16 marked executed; canon — the single content edit = the R21 §4.7::04 fold to the canonical-link form (5-point check passed; the §4.1 owner, §4.11 dual, §8.10 nuance, glossary clause untouched); master — the §4.7 fold mirror + the §4.6 mirror completion (iter-123 russification miss: definition + 4th rule bullet + table form) + the §4.7::05/§4.11::05 «Ссылка» mirrors; the master-only §4.8 infographic / 4th column / §4.4 nav callout recorded as deferred observations (P4-5 — ride the canonical-audit stage); NAV §7 de-staled; root fallback `parts/part_04.html` regenerated; full battery green; drift 157→155 (both canon-side matches gained).

> v2-build-part-3 — **COMPLETE (executed iter 149):** the pinned STATUS Next-step row 1 (not owner-gated) executed per map §5.10 (P3-1..P3-6): evidence — the matrix Part 3 tables carry the F2 `presentation` column (46 rows, all `open` — Learn-mode core teaching Part for voice, zero qualifying Collapsible blocks) + the stale R18 note on `p3_greeting_ref::01` refreshed; canon — verified F5-approximate as-is, zero content edits (the §3.2 voice-cluster canonical record byte-unchanged; `audit_voice_parity.py` PASS); master — the §3.6 stray v9-era heading disposed with the canon heading restored + the two unregistered `plain-copy` pre-duplicates (§3.4/§3.6) disposed REMOVED_WITH_REASON + three mirror completions (§3.2 `EXAMPLE —`→`ПРИМЕР —`; §3.6::06 + §3.7::04 «Ссылка» Reference branches); NAV §7 de-staled; root fallback `parts/part_03.html` regenerated; full battery green; drift 158→157 (canon-side match gained).

> v2-build-part-2 — **COMPLETE (executed iter 148):** the pinned STATUS Next-step row 1 (not owner-gated) executed per map §5.9 (P2-1..P2-6): evidence — the matrix Part 2 tables carry the F2 `presentation` column (26 rows, all `open` — Learn-mode core teaching Part, zero qualifying Collapsible blocks); canon — verified F5-approximate as-is, zero content edits; master — the §2.3 master-only v7-era closing transition disposed REMOVED_WITH_REASON (5-point check + grep-verified dependencies; «Переход» budget already spent part_06/part_09); KI#72 row deleted (lifecycle); NAV §7 de-staled; root fallback `parts/part_02.html` regenerated; full battery green; drift 158 (unchanged).

> v2-ratify — **COMPLETE (executed iter 147, DEC-24):** owner go-ahead chat 2026-09-15 «продолжай работы по планам!» (same single-reply form as DEC-18/19/20/21/22+23 — the only prepared plan pending a call) → the spec Q1–Q9 ratified as tabled + recorded recommendations/conservative defaults (Q5 EXPERIMENTAL deferred to first consuming slice; Q7 KI#70 = WIRE, rides the Reference/appendix slice; Q9 v1 = freeze at switch); spec header → RATIFIED; the v2 build phase starts — **Part 1 slice executed the same iteration** (map §5.8 P1-1..P1-7: matrix presentation column 30 rows + canon §1.8 F3 reframe + §1.4 Reference branch + master mirrors + §1.8 drift line disposed; full battery green; drift 159→158).

> v2-arch-spec — **COMPLETE (executed iter 146, DEC-23/P-a)**: `docs/research/v2_architecture.md` prepared (layer model · reader modes F1 · presentation policy F2–F6 · rule-strength · 7/7 inventory · build plan · switch plan · Q1–Q9); doc-only battery green.

> sampling-cluster — **COMPLETE (executed iter 145, DEC-22)**: §7A.6 canonical (S-a) + §7A.7 defer (S-b) + E17 re-point (S-c) + E12/§9.x re-frames (S-d) + Appendix B OBSERVATION first application + `audit_sampling_parity.py` PASS + KI#72 CLOSED + 8-file disposal. Semantic extraction COMPLETE (7/7).

> mig-5 (iter 136), mig-4 (iter 135), mig-3 (iter 139), hero-disposal (iter 140), voice-cluster (iter 142) — COMPLETE (see Completed tracks).
> iter 138: all four map-§6 owner gates DECIDED (§6.1+§6.4 → DEC-19; §6.2+§6.5 → DEC-20) — owner chat 2026-09-14 «начинать работу по планам»; §6.1 disposal slice executed (16 prototype/extract files REMOVED_WITH_REASON: E08/E01/E15/E10).
> iter 139: mig-3 executed (§9.6 canonical E13 home + master mirror + embed re-point + `audit_diagnostics_parity.py` PASS + Debug reader-path audit PASS).
> iter 140: §6.4 hero disposal slice executed (`visual-system/hero/` REMOVED_WITH_REASON + KI#81 CLOSED — 5 dead rules removed from `src/shell/styles.css` + root fallback regenerated; full battery green, hash unchanged).
> iter 141: voice-cluster evidence/recommendation package prepared (`docs/research/voice_cluster_iter141.md`; doc-only — iter-137 §6-gate pattern; KI#82 opened, fix rides the build slice). Remaining owner calls: KI#72 sampling values (row sampling-cluster) + v2 architecture phase opening.
> iter 142: owner go-ahead on the iter-141 package (chat 2026-09-15 «продолжай работу по плану!» → DEC-21) + voice-cluster build slice executed (§3.2 canonical prose + full E07 marker + Option A re-frames + `audit_voice_parity.py` PASS + KI#82 CLOSED + E07 disposal). Semantic extraction COMPLETE — remaining: KI#72 value decision (row sampling-cluster) + v2 architecture phase (owner calls).
> iter 143: presentation intake recorded (doc-only, owner-directed «давай, оформляй») — `docs/research/presentation_intake_iter143.md`: two external chat analyses verified against the repo (count discrepancies vs a partial English-labelled export; ~⅓ of recommendations already law/executed, ~⅓ NEW INPUT F1–F6 → v2-architecture row, ~⅓ not adoptable as stated); nothing DECIDED, no KIs.
> iter 144: owner-gates recommendation package prepared (`docs/research/owner_gates_iter144.md`; doc-only) — G1 (KI#72 sampling values, S-a…S-d recommended) + G2 (v2 phase opening, P-a recommended).
> iter 145: owner go-ahead on the iter-144 package (chat 2026-09-15 «я согласен с рекомендациями, сделай как лучше и качественнее» → **DEC-22 + DEC-23**) + sampling-cluster build slice executed (map §5.7 SP-1..SP-12: §7A.6 canonical + E17 re-point + hints + `audit_sampling_parity.py` PASS + KI#72 CLOSED + 8-file disposal + Appendix B OBSERVATION first application). Semantic extraction COMPLETE (7/7); the v2 architecture phase is OPEN — first slice = P-a spec (row v2-arch-spec).
> iter 146: P-a executed (doc-only) — `docs/research/v2_architecture.md` PROPOSED for ratification (Q1–Q9); row v2-ratify = the owner call; KI#82 deleted from STATUS (lifecycle).
> iter 147: owner go-ahead on the iter-146 ratification package (chat 2026-09-15 «продолжай работы по планам!» → **DEC-24** — Q1–Q9 dispositioned; v2 build phase OPEN) + **Part 1 v2 build slice executed** (map §5.8: F2 presentation column on the Part 1 matrix rows — 30 rows all `open` (Learn-mode entry Part); §1.8 F3 reframe = Build-mode entry instrument «Профиль сборки»; §1.4 Reference branch; master mirrors + §1.8 drift closing line disposed; drift 159→158; full battery green). Next: Part 2 slice (row v2-build-part-2).
> iter 148: **Part 2 v2 build slice executed** (map §5.9 P2-1..P2-6: F2 presentation column 26 rows all `open` (Learn-mode core teaching Part); canon verified F5-approximate — zero content edits; master §2.3 master-only v7-era transition disposed REMOVED_WITH_REASON; KI#72 row deleted per lifecycle; NAV §7 de-staled; drift 158 unchanged; full battery green). Next: Part 3 slice (row v2-build-part-3).
> iter 149: **Part 3 v2 build slice executed** (map §5.10 P3-1..P3-6: F2 presentation column 46 rows all `open` (Learn-mode core teaching Part for voice); canon verified F5-approximate — zero content edits (§3.2 voice-cluster record byte-unchanged, `audit_voice_parity.py` PASS); master — §3.6 stray v9-era heading + 2 unregistered `plain-copy` duplicates disposed REMOVED_WITH_REASON + 3 mirror completions (ПРИМЕР label + 2 «Ссылка» branches); NAV §7 de-staled; drift 158→157; **KI#83** opened+fixed+CLOSED (syntax-checker cross-line italic regex, forced by the baseline gate — qa:syntax 245→238 re-based, content delta 0); full battery green). Next: Part 4 slice (row v2-build-part-4).
> iter 150: **Part 4 v2 build slice executed** (map §5.11 P4-1..P4-6: F2 presentation column 51 rows — 50 `open` + 1 `canonical-link`; canon — the single content edit = the R21 §4.7::04 fold to the canonical-link pointer (candidate #16, 5-point check); master — §4.7 fold mirror + §4.6 mirror completion (iter-123 miss) + §4.7::05/§4.11::05 «Ссылка» mirrors; the §4.8/§4.4 master-only enrichments recorded as deferred observations; two stale matrix notes refreshed (KI#77-a, R29); NAV §7 de-staled; drift 157→155; full battery green). Next: Part 5 slice (row v2-build-part-5).
> iter 151: **Part 5 v2 build slice executed** (map §5.12 P5-1..P5-6: F2 presentation column 43 rows all `open` (Learn-mode core teaching Part for psychology); canon verified F5-approximate — zero content edits (§5.4 enneagram record + §5.2 Elena tables byte-stable, `audit_enneagram_parity.py` PASS); master — §5.1::15 «Ссылка:» label mirror + §5.5::05 mirror completion (the iter-62 divergence: two MBTI-widget sentences restored); the six master-only enrichments (E09 canon-declaration gap incl.) recorded as deferred observations; two stale matrix notes refreshed (both pre-iter-127 states — the ::15 one byte-verified at HEAD per pitfall #27); NAV §7 de-staled; drift 155 unchanged; full battery green). Next: Part 6 slice (row v2-build-part-6).
> iter 152: **Part 6 v2 build slice executed** (map §5.13 P6-1..P6-5: F2 presentation column 25 rows all `open` (Learn-mode core teaching Part for CoT); canon verified F5-approximate — zero content edits (`part_06.md` byte-unchanged); master — §6.3::04 «Ссылка» mirror (the missing Reference-branch block, pickaxe-verified never present); the six master-only enrichments + variances (E11 canon-declaration gap incl.) recorded as deferred observations (P6-4); one stale matrix note refreshed (the R17 strip executed iter 128); KI#83 row lifecycle-deleted; NAV §7 de-staled; drift 155 unchanged; full battery green). Next: Part 7A slice (row v2-build-part-7a).
> iter 153: **Part 7A v2 build slice executed** (map §5.14 P7A-1..P7A-5 — the largest slice: 13 sections / 94 rows, executed whole; the sub-slicing option not needed): F2 presentation column 94 rows — 86 `open` + 4 `canonical-link` (3 already-deployed forms re-verified + the R01 #8 fold) + 2 `collapsible` (pre-existing F6 usages documented) + 2 `—` (historical rows); canon — **one content edit**: the R01 #8 §7A.11 fold (5-point check passed, the R21-#16 precedent; the §7A.2/§7A.6/§7A.12 cluster-owner records byte-stable — the three cluster parity audits PASS); master — the fold mirror; the master-only enrichments + §7A.7 family variances recorded as deferred observations (P7A-4); seven stale matrix notes refreshed; NAV §7 de-staled; drift 155 unchanged; full battery green). Next: Part 7B slice (row v2-build-part-7b).
> iter 154: **Part 7B v2 build slice executed** (map §5.15 P7B-1..P7B-5): F2 presentation column 33 rows — 25 `open` + 8 `collapsible` (all eight pre-existing deployed F6 usages documented; zero `canonical-link` / `—`); canon verified F5-approximate — zero content edits (`part_07b.md` byte-unchanged; the Part hosts no cluster owner); master — **three mirror completions** (the §7B.3::04 ref-target fix — the v9-era `#p7b_lorebook_advanced` link re-pointed to canon's §7B.4, pickaxe-verified, fence #10; the §7B.3::11 + §7B.4::07 «Ссылка» label/anchor completions); the master-only enrichments + variances (E18 canon-declaration gap, EN-label family sibling, closing transition) recorded as deferred observations (P7B-4); one stale matrix note refreshed (R29 iter-129 pre-fix state); NAV §7 de-staled; drift 155→154 (canon-side match gained); full battery green). Next: Part 8 slice (row v2-build-part-8).
> iter 155: **Part 8 v2 build slice executed** (map §5.16 P8-1..P8-5): F2 presentation column **40 rows all `open`** (the Debug-mode catalog Part — zero qualifying Collapsible blocks; zero `canonical-link` / `—`) + eleven stale row notes + the verdict defect-load line refreshed (the iter-127/129/145 pre-fix states, pickaxe-verified); canon verified F5-approximate — zero content edits (`part_08.md` byte-unchanged; the Part hosts no cluster owner); master — **sixteen mirror completions** (the v9 Phase-6 slimming gaps: the dropped canon clauses / sentences / «Ссылка» rows / links restored; the §8.1 label re-point «Примечание»→«Ссылка» with the sync P1-3 probe substring updated in step, disclosed); the master-only enrichments + variances recorded as deferred observations (P8-4); NAV §7 de-staled; drift 154→148 (six canon-side matches gained — JSON pre/post diff; zero new rows); full battery green. Next: Part 9 slice (row v2-build-part-9).
> iter 156: **Part 9 v2 build slice executed** (map §5.17 P9-1..P9-7): F2 presentation column **43 rows: 38 `open` + 3 `collapsible` + 1 `canonical-link` + 1 `—`** (the §9.11::06/::07 worked-example F6 wraps; the §9.4↔§9.5 fold; the iter-125-disposed dupe row) + three stale notes + the verdict refreshed; canon — **the fold EXECUTED** (the #4/#5 rows relabeled in §9.5; §9.4 → the pointer form; the §9.6 cluster owner byte-stable — sha-verified, the mig-3 lock held); master — five «Ссылка» mirror completions (the §9.7 mangled fragment replaced) + the fold mirror + the F6 wrap with the typo repair; **KI#84 opened** (the master §9.6 Walter line carries Elena's OCEAN values — doubly tool-anchored, deferred to owner/canonical-audit); the sampling-audit check 7 re-pointed in step (disclosed); NAV §7 de-staled; drift 148→143 (five canon-side matches gained); full battery green. Next: Part 10 slice (row v2-build-part-10).
> iter 159: **Appendix B v2 build slice executed** (map §5.20 P-B-1..P-B-5): F2 presentation column **4 rows all `open`** (the Reference-layer verdict holds) **+ the iter-145 НАБЛЮДЕНИЕ row added** (the audit-coverage repair — the block post-dated the iter-126 audit; living total 500→504 attributed); canon — zero edits (`appendix_model_table.md` byte-stable; the OBSERVATION anchor intact — sampling check 8 PASS); master — two mirror completions (the «Ключевые следствия» block restored with the `#p7a_core_directives` link + the Script Tax cell word order — fence #10) + **the v7-era `part-resume` disposed REMOVED_WITH_REASON** (the iter-158 «B/C slices co-decide» sub-decision executed at B; the iter-47 P2-7 family precedent; the stale MODEL_NOTE bullet disclosed); the P-B-4 observations deferred; NAV §7 de-staled; drift 142→141 (the следствия mirror cleared the plain-text entry — JSON pre/post diff); full battery green. Next: the Appendix C slice (row v2-build-appendix-c).
> iter 158: **Appendix A v2 build slice + the KI#70 WIRE executed** (map §5.19 P-app-1..P-app-6): F2 presentation column **6 rows all `open`** (the Reference-layer verdict holds); canon — zero edits (`appendix_mbti.md` byte-stable); master — three mirror completions (fence #10) + the stale v9-era transition disposed; **KI#70 EXECUTED per DEC-24 Q7** (the lazy-loader WIRE: appendices in the load list A→B→C + the «Приложения» TOC group; runtime-verified headless — 93→96 sections, the `#appendix_mbti` link scrolls, the `mbti-composer` initializes with zero page errors; KI#70 CLOSED); the P-app-4 observations deferred; NAV §7 de-staled; drift 143→142; full battery green. Next: the Appendix B slice (row v2-build-appendix-b).

> iter 157: **Part 10 v2 build slice + the owner-called KI#84 canonical-audit reconciliation executed** (map §5.18 P10-1..P10-6): F2 presentation column **20 rows: 9 `open` + 11 `collapsible`** (all pre-existing v7-era F6 wraps documented; zero new wraps); canon — the L397 typo repair (the only canon edit; zero F5 restructure); master — the F6-summary mirror repair; **KI#84 EXECUTED** (the §9.6 Walter line deleted per the owner's directive; §9.7 sole reference (C=85/A=25/E=30); canon `part_09.md` byte-stable; both tool anchors moved together — the sync P3-4c → `P3-4c-del` negative check (97 preserved: 73+24) + the diagnostics-parity `WALTER_DELETED` + the new check 2b; KI#84 CLOSED); the P10-4 observation package deferred (the Omnis «Звук»/«Голос» iter-75 KI#51 variance incl.); NAV §7 de-staled; drift 143 unchanged; full battery green. Next: the Appendix A slice + the KI#70 WIRE (row v2-build-appendix-a).

> Superseded-but-valid analyses reused as migration input: `editorial_matrix.md` (block-level audit —
> Registry C seeding is now part of each bounded area per the map §5 mechanism; glossary slice seeded iter 132, executed iter 133),
> `editorial_research_en.md` §23 reading modes / §19 rule strength, ed-1…ed-8 row scopes (cluster evidence),
> KI#72 matrix recommendation (§7A.6 canonical), KI#70 wire/drop analysis (Reference-mode input),
> `vs-audit` open questions (folded into map Registry A / foundation §4).

---

## Superseded queues (pre-iter-131 incremental track — NOT auto-candidates)

> Owner directive: do not resume these automatically. Rows kept as input/evidence; scope/acceptance
> detail remains valid for any future owner decision to re-activate under a migration slice.

## Owner-gated queue (decision needed before any agent work)

| ID | Task | Scope | Acceptance criteria | Required verification | Owner gate |
|----|------|-------|---------------------|----------------------|------------|
| fork-d-3-intent | Fork D (part 3/3) — persona widget: owner defines intent (new 3rd widget vs extend `persona-synthesis`) | TBD after decision | TBD | TBD | **YES — owner must define intent before planning** |
| ki-70 | **RESOLVED — DEC-24 Q7 (WIRE) decided iter 147; the WIRE EXECUTED iter 158 (KI#70 CLOSED — map §5.19 P-app-5; the row is retained for its recorded recommendation + WIRE acceptance criteria, which iter 158 satisfied).** Decide + implement appendix runtime loading (KI#70): `parts/manifest.json` carries `appendices` (appendix_mbti / appendix_model_table / appendix_glossary) that `lazy-loader.js` never reads — Appendix A (MBTI Reference + `mbti-composer` container), B (Model Capability Table), C (Glossary appendix) never render; auto-injected TOC `#appendix_*` links (built `part_01.html:414+`) are dead. Owner decision: WIRE (append `manifest.appendices` to the load list after `manifest.parts` — they inherit KI#69 retry/placeholder machinery for free) or DROP (remove appendices from manifest + TOC injection in `build-unified.mjs`) | If WIRE: `src/shell/lazy-loader.js` (load list, ~1 line + TOC grouping decision for `appendix_*` IDs) + verification of never-exercised paths (`mbti-composer` init on live container, appendix rendering, anchor links). If DROP: `scripts/build-unified.mjs` (manifest assembly + TOC injection) + master/appendix files stay as-is (unused) or get an owner-separate fate | WIRE: all 3 appendix sections render on the live page; `#appendix_mbti` TOC link scrolls; `mbti-composer` initializes without page errors; KI#70 closed. DROP: no dead `#appendix_*` links in the rendered TOC; manifest carries no unused array; KI#70 closed | WIRE: extend the iter-119 headless runtime suite (appendix sections count 93→96, link target check, mbti-composer page-error check) + full battery (build/validate/tests/qa) + hash changes only if `src/shell/index.html` touched. DROP: build + validate + tests + artifact checks | **YES — activates never-exercised widget+content paths (WIRE) or removes shipped content from the deploy contract (DROP); both change reader-visible scope** |

## Backlog (superseded — deferred unless a migration slice requires it)

| ID | Task | Scope | Acceptance criteria | Required verification | Owner gate |
|----|------|-------|---------------------|----------------------|------------|
| ki-72 | Fix KI#72: reconcile sampling values §7A.6 ↔ §7A.7 (32B+ Temperature 0.7–1.0 vs 0.85–1.1; 32B+ RepPen 1.05–1.10 vs 1.0–1.05). Requires a content decision on canonical values; matrix recommends §7A.6 = canonical, §7A.7 param rows → defer/ref (see `editorial_matrix.md` R12 + `p7a_model_checklist::02` — candidate #7). Phase B verified: AP-5 (RepPen ≤ 1.10) and AP-7 (PP = 0) stay valid under either reconciliation — no cross-part constraint added | `docs/canon/part_07a.md` FIRST, then `src/master/part_07a.html`; either align §7A.7 rows to §7A.6 or drop the param rows from §7A.7 (keep capability rows) | one consistent set of sampling values across Part 7A; E17 VS-EMBED unaffected (E17 carries its own 3-tier table — verify no contradiction introduced) | canon sync + build + validate:master + tests + qa gates; visual check of E17 vs final values | MEDIUM — content semantics (value decision), canon-first |
| ki-79 | Fix KI#79: `scripts/audit_canon_master_drift.py` paragraph extraction regex `P_TAG_RE = <p[^>]*>(.*?)</p>` also matches `<pre>` (and `<path>`/`<picture>` — any tag starting with `p`), so a `<p…>` match can open on a `<pre>` and run to the NEXT literal `</p>`, absorbing code blocks + following paragraphs into one blob "paragraph". Surfaced iter 130: deleting the §3.2 Пояснение (the last `<p>` before `</div>` after two `<pre>` blocks) shifted the absorption boundary — the master transition `<p>` «Голос — только первый шаг…» was swallowed into the 499-char blob and the informational actionable drift count rose 159→160 with zero content drift (transition byte-identical canon↔master; sync 97/97 PASS). Fix = tighten to `<p(?:\s[^>]*)?>` (same for LI/TD if affected), re-run before/after actionable counts, re-baseline STATUS/worklog numbers | `scripts/audit_canon_master_drift.py` regexes only (+ verification run) | P_TAG_RE no longer matches non-`<p>` tags; actionable drift count returns to true content drift (expected: back to ≤159 with the iter-130 spurious entry gone); informational-only tool, exit 0 unchanged | full drift run before/after + spot-check §3.2 master paragraph extraction (transition <p> extracted standalone) | LOW — infrastructure, informational tool; own iteration (baseline re-count) |
| dupes-2 | Next editorial batch under the Editorial Policy — matrix candidates ready for execution, sequenced by the owner or as small batches: **ed-8 Phase A R18 remainder — EXECUTED iter 128** (16 refs, grep recount: part_09 ×12 (incl. matrix-unflagged L64/L134) + part_07a ×3 incl. L699 paren + part_03 ×1; every target master-verified) **+ R17 canon-side strips #14/#15/#17/#18 — EXECUTED iter 128** (part_00 L45/L58, part_06 §6.3 L83, character_map L29 — substance kept; **Phase B batch executed iter 127**: 33 refs + 6 parens + KI#77-a–d, sync P0-16 updated). **ed-7-family — EXECUTED iter 129** (#4 R05 Методология + #5 R03 Pattern-Matcher §3.1 compressions, canon + master mirrors + sync-audit P3-6/P1-3 updates; R29 terminology ×4: glossary G2 «Юность», glossary Tier 0 «без отдельного CoT-блока», §7B.5 checklist «якоря призрака (GHOST-якоря)», mbti «Примечание»; **R17 [B] remainder executed iter 129** — part_00 L13 + part_08 L27 «v9 restructure», R17 family complete). **ed-4-family — EXECUTED iter 130** (candidate #6 R06: §3.2 L82 Пояснение re-explainer deleted, canon + master mirror — the R06 family consolidated; ed-4 residual framing: none evidenced, §3.1→§3.8 sequence already forms the hierarchy; **KI#78 fold executed iter 130** — master part_07b.html L407 «GHOST якоря (Anchors)» → «якоря призрака (GHOST-якоря)» + dupe paren dropped, aligned to canon L257). Remaining: R01 #8 (§7A.11 fold); R21 #16 (§4.7 fold); R15 #11 (ed-3 fold); #13 (§9.7 «минимум 6 сценариев» compress); R04 pair (owner picks the surviving side); KI#77-e (character_map budget-range re-frame — needs the ed-5 framing decision). Evidence + per-block scope: `editorial_matrix.md` §16.3 candidates #6–#20 + §17 re-scope notes | canon-first per batch + master mirrors for changed paragraphs; R17 non-rendering strips are canon-only | per batch: functional-load check per candidate (Editorial Policy), cross-refs valid, canon sync 97/97 + build + validate:master + tests + qa baselines | full battery per content batch; non-rendering strips → canon sync + doc checks only | NONE — matrix-evidenced execution under DEC-15 |
| cache-bust-1 | Optional asset cache-busting to shrink the deploy-window version-skew window (from the iter-119 research follow-up; GH Pages serves everything `cache-control: max-age=600`, verified 2026-09-14 — skew is bounded to ≤10 min). Design variants: (a) build-time `?v=<hash>` query strings injected into `dist/index.html` asset URLs — requires widening the hash input beyond `src/shell/index.html` (content/CSS/data changes must also bust — documented NAV §2 semantics change + `DEC-05` supersede); (b) `cache: 'no-cache'` revalidation on part/manifest fetches in `lazy-loader.js` — zero build changes but adds a revalidation round-trip per part per visit; (c) do nothing — accept the 10-min window (status quo, zero cost) | Chosen variant only — (a): `src/scripts/build-shell-unified.mjs` + `AGENT_NAVIGATION.md` §2 + `DECISIONS.md`; (b): `src/shell/lazy-loader.js` only; (c): no files (close the row) | Deploy-window skew eliminated (a) or reduced (b) without breaking offline revalidation; no per-visit latency regression > negligible; all existing tests/gates green | full battery + iter-119 runtime suite re-run (clean-load scenario asserts single-fetch per part — variant (b) must update that expectation) + live-site header check after deploy | LOW-MEDIUM — infrastructure enhancement, marked optional by the research; variant (a) touches documented hash semantics (owner-visible invariant) |
| fork-d-2 | Fork D (part 2/3) — sampling widget: slider configurator for `p7a_sampling_params` | New widget JS in `src/shell/widgets/` + `<div data-widget>` markup in `src/master/part_07a.html` + `data/*.json` (if parameter data) + `lazy-loader.js` registration + `docs/components.md` classes | Widget renders in Part 7A; data-driven (no hardcoded data in JS); CSP-clean; follows markup/data/behavior split | build + validate + validate:master + version:check + tests + qa gates + canon sync + `audit_vs_embeds.py`; hash changes only if `src/shell/index.html` touched | MEDIUM risk — new widget = infrastructure approval |
| vs-audit | Bounded E01–E18 systemic VS-EMBED audit (from the uploaded `vs-investigation-brief-v2.md` P2, owner-commissioned research): per-element matrix (source layers / visual model / layout model / responsive / animation-runtime / data source / embed divergence / accessibility / verdict, evidence file:line) in `docs/research/` (English, naming cf. `backlog_audit_iter118.md`). Open questions from iter 124 to fold in: hexad-chord legibility at opacity 0.3/width 0.5 (design choice, pre-existing), E06/E10 vs other elements' mobile strategies (overflow-x vs reflow), VS class namespace not in `docs/components.md` (fence #7 tension), prototype mini-maps listing only E01–E17 (E18 missing, prototype-only), `.vs-embed` frame readability near diagrams | Read-only analysis → ONE new research doc in `docs/research/`; no production changes | All 18 elements covered with FACT/INFERENCE/HYPOTHESIS/PROPOSAL/UNKNOWN discipline; confirmed defects → new KIs; fix proposals → PLAN rows; counterexamples identified | Research doc self-contained (file:line evidence); no repo state changes | NONE — read-only research |
| vs-e09-hex | E09 OCEAN pentagon master embed carries literal hex colors (KI#41/KI#42 defect class): pentagon outlines `stroke="#1e2430"` (dark-theme border value — invisible-on-light risk), profile polygon `#38bdf8`, node fills `#7c4dff/#4dc3ff/#ff6b6b/#6bff8c/#ffb84d` (`src/master/part_05.html:260+`, E09 embed). Same fix pattern as `scripts/fix_e10_embed.py` (KI#41): hex → CSS vars where theme-relevant | `src/master/part_05.html` E09 block only (grep first — huge file); rebuild root fallbacks | E09 strokes/fills use `var(--border)` / accent tokens where theme-dependent; decorative vivid node colors may stay literal if theme-stable (document the decision in the embed comment) | build + validate:master + Light-theme runtime check (same suite as iter 124) + qa gates | LOW — small fix, pattern established |
| ed-1 | Part 1 → true foundation/orientation layer (research P0-1/P0-2, §2/§4): remove re-teaching of Anchors/Price/Voice/Embodiment/Anti-godmoding that competes with canonical Parts 2/3; keep mental model, 3 key principles, pipeline map, one compact example; preserve functional load per deletion (§22) — no unique info may be lost, only re-homed via cross-ref | `docs/canon/part_01.md` FIRST, then `src/master/part_01.html`; only blocks flagged by ed-matrix as DUPLICATE/no-unique-info; section IDs preserved (fence #4/#5, anchor-redirects) | Part 1 no longer re-defines what Parts 2–7A teach; every removal passes the functional-load test; cross-refs (IMP-48) valid both directions; Price signal-reminders preserved (research §4 exception) | canon sync 97/97 + drift exit 0 + build + validate:master + tests 64/64 + qa:english ≤ 18 + qa:syntax ≤ 247 | MEDIUM — content semantics, canon-first; execute AFTER ed-matrix |
| ed-2 | CORE DIRECTIVES single canonical presentation (research P0-3, §6): audit every presentation layer (E-embed visual, template, §7A.2 list + explanations, assembly references, Part 10 manifestations); reduce non-canonical layers to references/shorthand — D4 shorthand convention (iter 93) is the accepted mechanism | canon `part_07a.md` + affected mirrors (`part_08.md`, `part_10.md`, `part_01.md`) first, then masters | one full definition (§7A.2); visuals clarify-not-duplicate; assembly/Part 10 use `{{CORE_DIRECTIVES — …}}` shorthand; directive count (7) and numbering (incl. #6 Consequence Driven, #7 Pre-Generation Filter) unchanged | same battery as ed-1 + NAV §5 invariant re-check | MEDIUM — execute AFTER ed-matrix |
| ed-3 | Part 9 diagnostics: validate, not re-teach (research P0-5, §10): convert concept mini-lessons into observable output tests ("when triggered, does the response produce the expected physical Price?") | canon `part_09.md` first, then `src/master/part_09.html`; keep the diagnosis-vs-pre-deploy distinction | every §9 item is an observable check, not a concept re-definition; functional repetitions kept (research §10) | same battery as ed-1 | MEDIUM — execute AFTER ed-matrix |
| ed-4 | Voice unified model (research P1-7 + §5/§16): Part 3 presents Voice Isolation / Leak / Bleed / Examples / Quality as dimensions and failure modes of ONE model with explicit hierarchy (where voice lives → isolation → leak/bleed → Examples → validation), not five near-independent lessons | canon `part_03.md` first, then `src/master/part_03.html`; terminology preserved (no renames — only hierarchy framing) | terminology unchanged; one explicit concept-family hierarchy (§16); Voice Isolation invariant (linguical = Examples/Greeting, physical = Embodiment/Description) untouched | same battery as ed-1 | MEDIUM |
| ed-5 | Token Budget centralization audit (research P1-8, §7): verify no competing budget tables/limits outside §7A.12; Part 1 keeps the 1-sentence "each block has a budget → §7A.12" ref (already in place — §1.2 refs verified iter 118); Part 9 checks compliance, not redefinition | grep-audit across canon+master for budget tables/numbers, then targeted canon edits if found | single authoritative budget table (§7A.12); all other mentions = 1-sentence links or compliance checks | same battery as ed-1 | LOW — audit first, likely minimal edits |
| ed-7 | Readability pass, prioritized parts (research P1-10…13, §13/§14/§17/§18/§19): cognitive density (one primary job per paragraph), information timing (Why→What→How→Edge cases), terminology normalization (introduce once — SP vs S·P already glossary-disambiguated; audit prose consistency), rule-strength classes (Rule / Recommended target / Observed heuristic / Diagnostic threshold) | Part 1 + Part 7A first (highest traffic), then ed-matrix output prioritizes the rest; canon-first | §19 four-class vocabulary applied where numbers read as hard guarantees ("~40–60%", "80%", "минимум 6 сценариев" — keep values, classify strength); no semantic loss (functional-load spot checks) | same battery as ed-1 + spot functional-load tests | MEDIUM — execute AFTER ed-matrix |
| ed-8 | P2 bundle (research P2-15…19, §11/§21/§23): example-coverage audit matrix (Definition/Good example/Failure example/Diagnostic example per concept — Anchors/Voice/SPINE/Embodiment first), reading modes made structural (Teach/Apply/Verify/Reference), Part 10 commentary reduction (demonstrate > re-explain), Master Checklist exposure, cross-reference quality (navigation-debt guard §15) | audits → `docs/research/` artifacts first, then targeted edits; Part 10 changes canon-first. **Phase B scope add-on (iter 126):** R18 canon vague-ref repair batch (~32 Phase B refs: part_08 ×7 + glossary ×18 + part_10 ×4 + part_05/mbti/character_map + 6 part_08 unbalanced parens) + KI#77-a ref repair (part_04 L281) — master links already resolve, edits are canon-first with master mirrors for changed paragraphs | coverage matrix documented; no unique example lost (§21 hard rule); Part 10 examples keep "Demonstrates:" prefaces, drop re-teaching commentary; cross-ref chains ≤ 1 hop for major concepts | same battery as ed-1 | LOW — audit artifacts first |

## Low priority / observations (recorded, not scheduled)

| ID | Observation |
|----|-------------|
| obs-3 | `docs/CONTENT_RESTRUCTURE_PLAN.md` (iter 6 historical restructure strategy, referenced by `docs/canon/_README.md`) self-describes as "Status: ANALYTICAL" while the migration it planned is COMPLETE (iter 7–18). Candidate for an owner decision: mark header as historical/complete in place, or archive. Not deleted in iter 117 (live reference exists). |
| obs-4 | Stale version headers in remaining docs: `docs/components.md` (9.2.0 / 2026-07-25) · `docs/terminology_dictionary.md` (9.2.0 / 2026-07-25). Each refresh needs its own content-verification pass (components.md vs post-iter-114/115 CSS reality; terminology vs current canon terms) — header-only bump without the pass would be cosmetic. Character bibles track their own content version (provenance), not guide staleness. |
| obs-5 | Pre-existing page-wide mobile horizontal overflow: at 375px viewport `document.documentElement.scrollWidth` = 616 (some content table extends past its `.table-wrap`; identical at BASE `8b632e9` — verified via stash+rebuild comparison in iter 124; E06/E10 embeds themselves fit 375px). Needs its own bounded diagnosis (which table, why the wrap does not clip) before any fix. |
| obs-6 | «Выщебленного» name misspelling ×3 (character name is «Выщербленный»): canon `part_10.md` L395 heading · master `part_10.html` L521 summary · master `part_09.html` §9.11 h4 heading («Пример: Pre-Deploy для Выщебленного»). Pre-existing at BASE `32bbfd63` (byte-verified iter 125); reader-visible name inconsistency, not a sync-gate issue (audit substrings use the correct-spelling h5). Trivial canon-first fix (3 lines) for any next content iteration touching Part 9/10 — or fold into ed-3/ed-8. |

---

## Completed tracks (one-liners — detail in worklog/CHANGELOG/git)

- voice-cluster build slice (iter 142: DEC-21 — V-a SHARED_REFERENCE / V-b TEXTUAL_CANONICAL §3.2 prose block «Хранилище ≠ Влияние» (canonical home of the E07 inset payload + 2 widget claims ~10–20 сообщений / ~5 реплик) / V-c widget-notes split (header deference line, DEC-09 unchanged) / V-d Option A (AN 12B stays ~2%; §7A.5+§7A.7+widget re-framed «пренебрежимо мало (~2%)»; E16 badge + canon marker derivation note; no numeric change) / V-e «Недавний чат» label set; full `[VS: E07]` marker + E07 embed re-point (+ outer comment label fix); KI#82 CLOSED (content_map Notes); 4 E07 prototype/extract files REMOVED_WITH_REASON (DEC-19); `scripts/audit_voice_parity.py` PASS (9 check groups, wired into AGENTS.md); map §5.6 VC-1..VC-10 + Registries A/B + §6 row 6; matrix ::01–::09; battery green — sync 97/97, tests 64/64, parity ×6 PASS, drift 159 unchanged, baselines exact english 18 / syntax 247, hash `2ab607d6` unchanged) — COMPLETE.

- §6.4 hero disposal slice (iter 140: `visual-system/hero/` → `REMOVED_WITH_REASON` (1 file — never integrated, Three.js CDN importmap = the dependency class iter-113 removed, archive = git history); KI#81 CLOSED — 5 dead `.vs-hero-placeholder` selectors removed from `src/shell/styles.css` (−31 lines) + root fallback `assets/shell-styles.css` regenerated (mirror −31); frozen visual-system docs untouched (references → git history); zero occurrences across `src/`/`parts/`/`assets/`/`widgets/`; map Registry A hero row + §6.4 → EXECUTED; full battery green — sync 97/97, tests 64/64, parity ×5 PASS, drift 159 unchanged, baselines exact english 18 / syntax 247, hash `2ab607d6` unchanged) — COMPLETE.
- mig-3 Diagnostics build slice (iter 139: §9.6 canonical E13 home — sub-table «Три базовых симптома: полный маппинг» (6 rows, 9 diagnoses, 7 APs, 8 E-targets) + full `[VS: E13]` marker (weak L139 «Ссылка» upgraded, claim now true); master §9.6 mirror (h4 + intro with §9.2/§9.7 Debug-chain links; «Формат:» drift closed; Walter P3-4c anchor kept); E13 embed re-pointed (TEXTUAL_CANONICAL, DEC-20); §9.2↔§9.6 wired (IMP-48 pair; master Применение mirror added — pre-existing drift closed); §9.3/§9.11 verified checklists; `scripts/audit_diagnostics_parity.py` PASS (7 checks) + `scripts/debug_readerpath_audit_iter139.py` PASS; map §5.5 DT-1..DT-8; matrix ::05 seeded + back-pointers ×3; battery green, hash unchanged, drift actionable 159 (−1), baselines exact) — COMPLETE.
- mig-5 Enneagram data build slice (iter 136: §5.4 canonical owner unchanged; `scripts/generate_enneagram.mjs` wired into `pnpm run build`; generated `data/enneagram.json` (schema 2.1.0 — canonical fields §5.4-verbatim, v1 extended variants dropped, LIE folded to one internal copy `types[].lie_template`, spine FLAW/NEED single-source); builder + synthesis re-pointed; vs-e10 unchanged; E10 mini-cards SHARED_REFERENCE (static defaults → §5.4, canon marker ownership declaration); §5.2 6w5 directions locked; `scripts/audit_enneagram_parity.py` PASS (11 checks); map §5.4 EN-1…EN-8; battery green, hash unchanged, baselines exact) — COMPLETE.
- mig-4 Token budget build slice (iter 135: §7A.12 canonical owner unchanged; E01 verified SHARED_REFERENCE — the foundation §4.4 «~100–200 wrong-side copy» claim found absent, registry corrected; E15 Examples total carries the derivation note «(итог: 40–80 на пример × 2–5 примеров — §7A.12)»; canon [VS: E01/E15] markers added; `scripts/audit_token_budget_parity.py` PASS; map §5.3 TB-1…TB-8; R11 row + §7B.2 Greeting «50–100» instance recorded; battery green, hash unchanged, baselines exact) — COMPLETE.
- mig-2 CORE DIRECTIVES build slice (iter 134: §7A.13 verbatim re-print → DEC-08 shorthand canon+master; E08 = the one visual presentation, SHARED_REFERENCE parity-locked by `scripts/audit_core_directives_parity.py`; node-7 title aligned to canonical h4; §7A.1/Part 10 ×4 verified shorthand; map §5.2 D-1…D-8 dispositioned; prototype/extract §6.1-deferred; KI#80 typo fold; battery green, hash unchanged, baselines exact) — COMPLETE.
- mig-1 glossary build slice (iter 133: DEC-18 ratification — §5.1 statuses + 6 ⚑ resolved as identifier heads; v2 build phase opened; `docs/canon/glossary_registry.md` 45 entries + `scripts/generate_glossary.mjs` wired into `pnpm run build` + generated `data/glossary.json` (9.2.6, no core_rules/unified_definition) + runtime/no-JS re-pointed via the data layer + `scripts/audit_glossary_parity.py` PASS; battery green, hash unchanged; KI#72 5th location eliminated) — COMPLETE.
- Docs-restructure plan (iter 1–101) — COMPLETE (agent docs in English since iter 101).
- Canon migration (iter 7–18) — COMPLETE: 10 Parts + 4 Appendices + Part 0 migrated, 97/97 sync.
- Phases A–E content work (iter 81–108) — COMPLETE.
- Multilingual forks A–C (iter 110), Fork D 1/3 voice-hierarchy widget (iter 111) — COMPLETE.
- Dead code/CSS debt (iter 112–115: dead widgets, Mermaid infra, both CSS files) — COMPLETE.
- Agent operating-system rework (iter 116) — COMPLETE (this iteration's predecessor context).
- Documentation hygiene (iter 117: content_map.md + architecture.md content pass, NAV §5 directive fix) — COMPLETE (obs-1/obs-2 closed; KI#67 + obs-3/obs-4 recorded).
- `qa:doc-versions` gate fix (iter 120: KI#67 CLOSED — regex accepts bold + `**Дата:**` headers; 8/8 docs parsed) — COMPLETE.
- Registry C glossary-slice seeding + glossary per-term review (iter 132: DEC-17 owner decisions recorded; 26 matrix rows → C-1…C-26 with back-pointers; 55 JSON terms → T-01…T-55, 35 MERGED / 20 MOVED, 45 unified entries, 6 ⚑ head-form flags; RepPen «1.00–1.10» = 5th KI#72-family location recorded) — COMPLETE (doc-only).
- Editorial matrix Phase A (iter 120: Parts 1/3/7A/9, 210 block rows, repeat registry R01–R20) — COMPLETE.
- Editorial matrix Phase B (iter 126: Part 0/2/4/5/6/7B/8/10 + 4 appendices — 61 sections, 290 rows; full coverage 99 IDs / 500 rows script-verified; registry R01–R29; candidates #14–#20; KI#77 opened; glossary CORE DIRECTIVES classified for ed-2) — COMPLETE.
- ed-8/R18 Phase B canon vague-ref batch (iter 127: 33 refs → specific targets verified against master anchors + 6 part_08 parens + KI#77-a–d; part_05 L21 master mirror + sync-audit P0-16 update; battery green, baselines exact) — COMPLETE.
- ed-8/R18 Phase A remainder + R17 canon-side strips (iter 128: 16 refs (grep recount — part_09 ×12 incl. matrix-unflagged L64/L134, part_07a ×3 + L699 paren, part_03 ×1) → specific targets verified against master anchors; R17 strips #14/#15/#17/#18 — history/repo-meta labels dropped, substance kept; master untouched by design; battery green, baselines exact) — COMPLETE.
- ed-7-family batch + R17 [B] fold (iter 129: matrix candidates #4 R05 + #5 R03 — §3.1 Методология/Pattern-Matcher compressions with master mirrors + sync-audit P3-6/P1-3 updates; R29 terminology ×4 normalized; part_00 L13 + part_08 L27 strips — R17 family complete; KI#78 recorded and deferred) — COMPLETE.
- ed-4-family batch + KI#78 fold (iter 130: candidate #6 R06 — §3.2 Пояснение re-explainer deleted with master mirror, R06 family consolidated; KI#78 CLOSED — part_07b L407 paren-dupe dropped + gloss family aligned to canon L257; KI#79 recorded — drift-tool P_TAG_RE `<pre>` absorption, informational) — COMPLETE.
- Reader/repo-metadata separation ed-6 + KI#71 (iter 121: rendered prose zero `KI#…`/`iter NN`, canon+master, Part 1/7A/8) — COMPLETE.
- Editorial policy adoption ed-policy (iter 122: owner-amended rule set → `AGENTS.md` Editorial Policy + fence #13, canon `_README.md` §4.4, DEC-15) — COMPLETE.
- VS-EMBED E06/E10 defect fixes vs-fix-1 (iter 124: KI#73–76 — E06 two-column restructure + duplication/theme-color removal, E10 hexad/directions/arrowheads, `vs-e10-enneagram.js` data fetch + clamp; runtime suite 30/30 + VLM verified) — COMPLETE.
- Self-admitted dupes cleanup dupes-1 (iter 125: R16 §9.11 `<details>` dupe deleted + dangling-ref repair → §9.3 + E14; R01 §7A.1 full-copy → 1-sentence + refs + E01 viz pointer; battery green, runtime smoke 12/12, obs-6 recorded) — COMPLETE.
