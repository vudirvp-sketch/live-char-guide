# Live Character Guide — Plan & Backlog

> **What is pending, and in what order.** Order authority: `STATUS.md` **Next step** (re-pinned
> every iteration) picks from this backlog; owner-gated rows are not auto-candidates.
> Scope/acceptance live HERE — one row = one future iteration (or a numbered part of one).
> History of completed work: `worklog.md` · `CHANGELOG.md` · git. This file carries no history.
>
> Every backlog row MUST carry: Task · Scope · Acceptance criteria · Required verification · Owner gate (if any).

---

## ACTIVE TRACK (since iter 131): v1 → v2 architecture migration

> Owner track-switch directive (chat 2026-09-14): the incremental editorial cleanup/research track is
> **superseded**; Fork D 2/3, KI#70 (standalone), Fork D 3/3, KI#72 (as content decision), and `dupes-N`
> cleanup passes are **not auto-candidates**. Old work = input only where a direct migration dependency
> exists. **v1 is frozen as immutable migration source; v2 = editable target.**
> Migration order: `semantic extraction → v2 architecture → v2 build → parity audit → canonical audit →
> reader-path audit → switch`. Reader modes: **Learn → Build → Debug → Reference**.
> Evidence base: `docs/research/migration_foundation_iter131.md` · living registry: `docs/research/migration_map_v2.md`.

| ID | Task | Scope | Acceptance criteria | Required verification | Owner gate |
|----|------|-------|---------------------|----------------------|------------|
| mig-3 | **Bounded area — Diagnostics cluster** (Debug mode): E13 unique symptom→check→fix mappings vs §9.5/§9.6 canonicalization decision; §9.3/§9.11 checklist dispositions | blocked on the E13 decision (map §6.2): canonicalize mappings in §9.6 vs declare E13 VISUAL_CANONICAL | E13 payload either has a canonical textual home or a declared visual-canonical status; no competing diagnostic sources in the v2 slice | parity audit vs E13 inventory + Debug reader-path audit (symptom → cause → test → one-change → validation) | **YES — E13 disposition decision** |

> mig-5 (Enneagram data cluster) — COMPLETE (iter 136, see Completed tracks). mig-4 (Token budget cluster) — COMPLETE (iter 135, see Completed tracks).

> Superseded-but-valid analyses reused as migration input: `editorial_matrix.md` (block-level audit —
> Registry C seeding is now part of each bounded area per the map §5 mechanism; glossary slice seeded iter 132, executed iter 133),
> `editorial_research_en.md` §23 reading modes / §19 rule strength, ed-1…ed-8 row scopes (cluster evidence),
> KI#72 matrix recommendation (§7A.6 canonical), KI#70 wire/drop analysis (Reference-mode input),
> `vs-audit` open questions (folded into map Registry A / foundation §4).

---

## Superseded queues (pre-iter-131 incremental track — NOT auto-candidates)

> Owner directive: do not resume these automatically. Rows kept as input/evidence; scope/acceptance
> detail remains valid for any future owner decision to re-activate under a migration slice.

## Owner-gated queue (decision needed before any agent work)

| ID | Task | Scope | Acceptance criteria | Required verification | Owner gate |
|----|------|-------|---------------------|----------------------|------------|
| fork-d-3-intent | Fork D (part 3/3) — persona widget: owner defines intent (new 3rd widget vs extend `persona-synthesis`) | TBD after decision | TBD | TBD | **YES — owner must define intent before planning** |
| ki-70 | Decide + implement appendix runtime loading (KI#70): `parts/manifest.json` carries `appendices` (appendix_mbti / appendix_model_table / appendix_glossary) that `lazy-loader.js` never reads — Appendix A (MBTI Reference + `mbti-composer` container), B (Model Capability Table), C (Glossary appendix) never render; auto-injected TOC `#appendix_*` links (built `part_01.html:414+`) are dead. Owner decision: WIRE (append `manifest.appendices` to the load list after `manifest.parts` — they inherit KI#69 retry/placeholder machinery for free) or DROP (remove appendices from manifest + TOC injection in `build-unified.mjs`) | If WIRE: `src/shell/lazy-loader.js` (load list, ~1 line + TOC grouping decision for `appendix_*` IDs) + verification of never-exercised paths (`mbti-composer` init on live container, appendix rendering, anchor links). If DROP: `scripts/build-unified.mjs` (manifest assembly + TOC injection) + master/appendix files stay as-is (unused) or get an owner-separate fate | WIRE: all 3 appendix sections render on the live page; `#appendix_mbti` TOC link scrolls; `mbti-composer` initializes without page errors; KI#70 closed. DROP: no dead `#appendix_*` links in the rendered TOC; manifest carries no unused array; KI#70 closed | WIRE: extend the iter-119 headless runtime suite (appendix sections count 93→96, link target check, mbti-composer page-error check) + full battery (build/validate/tests/qa) + hash changes only if `src/shell/index.html` touched. DROP: build + validate + tests + artifact checks | **YES — activates never-exercised widget+content paths (WIRE) or removes shipped content from the deploy contract (DROP); both change reader-visible scope** |

## Backlog (superseded — deferred unless a migration slice requires it)

| ID | Task | Scope | Acceptance criteria | Required verification | Owner gate |
|----|------|-------|---------------------|----------------------|------------|
| ki-72 | Fix KI#72: reconcile sampling values §7A.6 ↔ §7A.7 (32B+ Temperature 0.7–1.0 vs 0.85–1.1; 32B+ RepPen 1.05–1.10 vs 1.0–1.05). Requires a content decision on canonical values; matrix recommends §7A.6 = canonical, §7A.7 param rows → defer/ref (see `editorial_matrix.md` R12 + `p7a_model_checklist::02` — candidate #7). Phase B verified: AP-5 (RepPen ≤ 1.10) and AP-7 (PP = 0) stay valid under either reconciliation — no cross-part constraint added | `docs/canon/part_07a.md` FIRST, then `src/master/part_07a.html`; either align §7A.7 rows to §7A.6 or drop the param rows from §7A.7 (keep capability rows) | one consistent set of sampling values across Part 7A; E17 VS-EMBED unaffected (E17 carries its own 3-tier table — verify no contradiction introduced) | canon sync + build + validate:master + tests + qa gates; visual check of E17 vs final values | MEDIUM — content semantics (value decision), canon-first |
| ki-79 | Fix KI#79: `scripts/audit_canon_master_drift.py` paragraph extraction regex `P_TAG_RE = <p[^>]*>(.*?)</p>` also matches `<pre>` (and `<path>`/`<picture>` — any tag starting with `p`), so a `<p…>` match can open on a `<pre>` and run to the NEXT literal `</p>`, absorbing code blocks + following paragraphs into one blob "paragraph". Surfaced iter 130: deleting the §3.2 Пояснение (the last `<p>` before `</div>` after two `<pre>` blocks) shifted the absorption boundary — the master transition `<p>` «Голос — только первый шаг…» was swallowed into the 499-char blob and the informational actionable drift count rose 159→160 with zero content drift (transition byte-identical canon↔master; sync 97/97 PASS). Fix = tighten to `<p(?:\s[^>]*)?>` (same for LI/TD if affected), re-run before/after actionable counts, re-baseline STATUS/worklog numbers | `scripts/audit_canon_master_drift.py` regexes only (+ verification run) | P_TAG_RE no longer matches non-`<p>` tags; actionable drift count returns to true content drift (expected: back to ≤159 with the iter-130 spurious entry gone); informational-only tool, exit 0 unchanged | full drift run before/after + spot-check §3.2 master paragraph extraction (transition <p> extracted standalone) | LOW — infrastructure, informational tool; own iteration (baseline re-count) |
| dupes-2 | Next editorial batch under the Editorial Policy — matrix candidates ready for execution, sequenced by the owner or as small batches: **ed-8 Phase A R18 remainder — EXECUTED iter 128** (16 refs, grep recount: part_09 ×12 (incl. matrix-unflagged L64/L134) + part_07a ×3 incl. L699 paren + part_03 ×1; every target master-verified) **+ R17 canon-side strips #14/#15/#17/#18 — EXECUTED iter 128** (part_00 L45/L58, part_06 §6.3 L83, character_map L29 — substance kept; **Phase B batch executed iter 127**: 33 refs + 6 parens + KI#77-a–d, sync P0-16 updated). **ed-7-family — EXECUTED iter 129** (#4 R05 Методология + #5 R03 Pattern-Matcher §3.1 compressions, canon + master mirrors + sync-audit P3-6/P1-3 updates; R29 terminology ×4: glossary G2 «Юность», glossary Tier 0 «без отдельного CoT-блока», §7B.5 checklist «якоря призрака (GHOST-якоря)», mbti «Примечание»; **R17 [B] remainder executed iter 129** — part_00 L13 + part_08 L27 «v9 restructure», R17 family complete). **ed-4-family — EXECUTED iter 130** (candidate #6 R06: §3.2 L82 Пояснение re-explainer deleted, canon + master mirror — the R06 family consolidated; ed-4 residual framing: none evidenced, §3.1→§3.8 sequence already forms the hierarchy; **KI#78 fold executed iter 130** — master part_07b.html L407 «GHOST якоря (Anchors)» → «якоря призрака (GHOST-якоря)» + dupe paren dropped, aligned to canon L257). Remaining: R01 #8 (§7A.11 fold); R21 #16 (§4.7 fold); R15 #11 (ed-3 fold); #13 (§9.7 «минимум 6 сценариев» compress); R04 pair (owner picks the surviving side); KI#77-e (character_map budget-range re-frame — needs the ed-5 framing decision). Evidence + per-block scope: `editorial_matrix.md` §16.3 candidates #6–#20 + §17 re-scope notes | canon-first per batch + master mirrors for changed paragraphs; R17 non-rendering strips are canon-only | per batch: functional-load check per candidate (Editorial Policy), cross-refs valid, canon sync 97/97 + build + validate:master + tests + qa baselines | full battery per content batch; non-rendering strips → canon sync + doc checks only | NONE — matrix-evidenced execution under DEC-15 |
| cache-bust-1 | Optional asset cache-busting to shrink the deploy-window version-skew window (from the iter-119 research follow-up; GH Pages serves everything `cache-control: max-age=600`, verified 2026-09-14 — skew is bounded to ≤10 min). Design variants: (a) build-time `?v=<hash>` query strings injected into `dist/index.html` asset URLs — requires widening the hash input beyond `src/shell/index.html` (content/CSS/data changes must also bust — documented NAV §2 semantics change + `DEC-05` supersede); (b) `cache: 'no-cache'` revalidation on part/manifest fetches in `lazy-loader.js` — zero build changes but adds a revalidation round-trip per part per visit; (c) do nothing — accept the 10-min window (status quo, zero cost) | Chosen variant only — (a): `src/scripts/build-shell-unified.mjs` + `AGENT_NAVIGATION.md` §2 + `DECISIONS.md`; (b): `src/shell/lazy-loader.js` only; (c): no files (close the row) | Deploy-window skew eliminated (a) or reduced (b) without breaking offline revalidation; no per-visit latency regression > negligible; all existing tests/gates green | full battery + iter-119 runtime suite re-run (clean-load scenario asserts single-fetch per part — variant (b) must update that expectation) + live-site header check after deploy | LOW-MEDIUM — infrastructure enhancement, marked optional by the research; variant (a) touches documented hash semantics (owner-visible invariant) |
| fork-d-2 | Fork D (part 2/3) — sampling widget: slider configurator for `p7a_sampling_params` | New widget JS in `src/shell/widgets/` + `<div data-widget>` markup in `src/master/part_07a.html` + `data/*.json` (if parameter data) + `lazy-loader.js` registration + `docs/components.md` classes | Widget renders in Part 7A; data-driven (no hardcoded data in JS); CSP-clean; follows markup/data/behavior split | build + validate + validate:master + version:check + tests + qa gates + canon sync + `audit_vs_embeds.py`; hash changes only if `src/shell/index.html` touched | MEDIUM risk — new widget = infrastructure approval |
| vs-audit | Bounded E01–E18 systemic VS-EMBED audit (from the uploaded `vs-investigation-brief-v2.md` P2, owner-commissioned research): per-element matrix (source layers / visual model / layout model / responsive / animation-runtime / data source / embed divergence / accessibility / verdict, evidence file:line) in `docs/research/` (English, naming cf. `backlog_audit_iter118.md`). Open questions from iter 124 to fold in: hexad-chord legibility at opacity 0.3/width 0.5 (design choice, pre-existing), E06/E10 vs other elements' mobile strategies (overflow-x vs reflow), VS class namespace not in `docs/components.md` (fence #7 tension), prototype mini-maps listing only E01–E17 (E18 missing, prototype-only), `.vs-embed` frame readability near diagrams | Read-only analysis → ONE new research doc in `docs/research/`; no production changes | All 18 elements covered with FACT/INFERENCE/HYPOTHESIS/PROPOSAL/UNKNOWN discipline; confirmed defects → new KIs; fix proposals → PLAN rows; counterexamples identified | Research doc self-contained (file:line evidence); no repo state changes | NONE — read-only research |
| vs-e09-hex | E09 OCEAN pentagon master embed carries literal hex colors (KI#41/KI#42 defect class): pentagon outlines `stroke="#1e2430"` (dark-theme border value — invisible-on-light risk), profile polygon `#38bdf8`, node fills `#7c4dff/#4dc3ff/#ff6b6b/#6bff8c/#ffb84d` (`src/master/part_05.html:260+`, E09 embed). Same fix pattern as `scripts/fix_e10_embed.py` (KI#41): hex → CSS vars where theme-relevant | `src/master/part_05.html` E09 block only (grep first — huge file); rebuild root fallbacks | E09 strokes/fills use `var(--border)` / accent tokens where theme-dependent; decorative vivid node colors may stay literal if theme-stable (document the decision in the embed comment) | build + validate:master + Light-theme runtime check (same suite as iter 124) + qa gates | LOW — small fix, pattern established |
| ed-1 | Part 1 → true foundation/orientation layer (research P0-1/P0-2, §2/§4): remove re-teaching of Anchors/Price/Voice/Embodiment/Anti-godmoding that competes with canonical Parts 2/3; keep mental model, 3 key principles, pipeline map, one compact example; preserve functional load per deletion (§22) — no unique info may be lost, only re-homed via cross-ref | `docs/canon/part_01.md` FIRST, then `src/master/part_01.html`; only blocks flagged by ed-matrix as DUPLICATE/no-unique-info; section IDs preserved (fence #4/#5, anchor-redirects) | Part 1 no longer re-defines what Parts 2–7A teach; every removal passes the functional-load test; cross-refs (IMP-48) valid both directions; Price signal-reminders preserved (research §4 exception) | canon sync 97/97 + drift exit 0 + build + validate:master + tests 64/64 + qa:english ≤ 18 + qa:syntax ≤ 247 | MEDIUM — content semantics, canon-first; execute AFTER ed-matrix |
| ed-2 | CORE DIRECTIVES single canonical presentation (research P0-3, §6): audit every presentation layer (E-embed visual, template, §7A.2 list + explanations, assembly references, Part 10 manifestations); reduce non-canonical layers to references/shorthand — D4 shorthand convention (iter 93) is the accepted mechanism | canon `part_07a.md` + affected mirrors (`part_08.md`, `part_10.md`, `part_01.md`) first, then masters | one full definition (§7A.2); visuals clarify-not-duplicate; assembly/Part 10 use `{{CORE_DIRECTIVES — …}}` shorthand; directive count (7) and numbering (incl. #6 Consequence Driven, #7 Pre-Generation Filter) unchanged | same battery as ed-1 + NAV §5 invariant re-check | MEDIUM — execute AFTER ed-matrix |
| ed-3 | Part 9 diagnostics: validate, not re-teach (research P0-5, §10): convert concept mini-lessons into observable output tests ("when triggered, does the response produce the expected physical Price?") | canon `part_09.md` first, then `src/master/part_09.html`; keep the diagnosis-vs-pre-deploy distinction | every §9 item is an observable check, not a concept re-definition; functional repetitions kept (research §10) | same battery as ed-1 | MEDIUM — execute AFTER ed-matrix |
| ed-4 | Voice unified model (research P1-7 + §5/§16): Part 3 presents Voice Isolation / Leak / Bleed / Examples / Quality as dimensions and failure modes of ONE model with explicit hierarchy (where voice lives → isolation → leak/bleed → Examples → validation), not five near-independent lessons | canon `part_03.md` first, then `src/master/part_03.html`; terminology preserved (no renames — only hierarchy framing) | terminology unchanged; one explicit concept-family hierarchy (§16); Voice Isolation invariant (linguical = Examples/Greeting, physical = Embodiment/Description) untouched | same battery as ed-1 | MEDIUM |
| ed-5 | Token Budget centralization audit (research P1-8, §7): verify no competing budget tables/limits outside §7A.12; Part 1 keeps the 1-sentence "each block has a budget → §7A.12" ref (already in place — §1.2 refs verified iter 118); Part 9 checks compliance, not redefinition | grep-audit across canon+master for budget tables/numbers, then targeted canon edits if found | single authoritative budget table (§7A.12); all other mentions = 1-sentence links or compliance checks | same battery as ed-1 | LOW — audit first, likely minimal edits |
| ed-7 | Readability pass, prioritized parts (research P1-10…13, §13/§14/§17/§18/§19): cognitive density (one primary job per paragraph), information timing (Why→What→How→Edge cases), terminology normalization (introduce once — SP vs S·P already glossary-disambiguated; audit prose consistency), rule-strength classes (Rule / Recommended target / Observed heuristic / Diagnostic threshold) | Part 1 + Part 7A first (highest traffic), then ed-matrix output prioritizes the rest; canon-first | §19 four-class vocabulary applied where numbers read as hard guarantees ("~40–60%", "80%", "минимум 6 сценариев" — keep values, classify strength); no semantic loss (functional-load spot checks) | same battery as ed-1 + spot functional-load tests | MEDIUM — execute AFTER ed-matrix |
| ed-8 | P2 bundle (research P2-15…19, §11/§21/§23): example-coverage audit matrix (Definition/Good example/Failure example/Diagnostic example per concept — Anchors/Voice/SPINE/Embodiment first), reading modes made structural (Teach/Apply/Verify/Reference), Part 10 commentary reduction (demonstrate > re-explain), Master Checklist exposure, cross-reference quality (navigation-debt guard §15) | audits → `docs/research/` artifacts first, then targeted edits; Part 10 changes canon-first. **Phase B scope add-on (iter 126):** R18 canon vague-ref repair batch (~32 Phase B refs: part_08 ×7 + glossary ×18 + part_10 ×4 + part_05/mbti/character_map + 6 part_08 unbalanced parens) + KI#77-a ref repair (part_04 L281) — master links already resolve, edits are canon-first with master mirrors for changed paragraphs | coverage matrix documented; no unique example lost (§21 hard rule); Part 10 examples keep "Demonstrates:" prefaces, drop re-teaching commentary; cross-ref chains ≤ 1 hop for major concepts | same battery as ed-1 | LOW — audit artifacts first |

## Low priority / observations (recorded, not scheduled)

| ID | Observation |
|----|-------------|
| obs-3 | `docs/CONTENT_RESTRUCTURE_PLAN.md` (iter 6 historical restructure strategy, referenced by `docs/canon/_README.md`) self-describes as "Status: ANALYTICAL" while the migration it planned is COMPLETE (iter 7–18). Candidate for an owner decision: mark header as historical/complete in place, or archive. Not deleted in iter 117 (live reference exists). |
| obs-4 | Stale version headers in remaining docs: `docs/components.md` (9.2.0 / 2026-07-25) · `docs/terminology_dictionary.md` (9.2.0 / 2026-07-25). Each refresh needs its own content-verification pass (components.md vs post-iter-114/115 CSS reality; terminology vs current canon terms) — header-only bump without the pass would be cosmetic. Character bibles track their own content version (provenance), not guide staleness. |
| obs-5 | Pre-existing page-wide mobile horizontal overflow: at 375px viewport `document.documentElement.scrollWidth` = 616 (some content table extends past its `.table-wrap`; identical at BASE `8b632e9` — verified via stash+rebuild comparison in iter 124; E06/E10 embeds themselves fit 375px). Needs its own bounded diagnosis (which table, why the wrap does not clip) before any fix. |
| obs-6 | «Выщебленного» name misspelling ×3 (character name is «Выщербленный»): canon `part_10.md` L395 heading · master `part_10.html` L521 summary · master `part_09.html` §9.11 h4 heading («Пример: Pre-Deploy для Выщебленного»). Pre-existing at BASE `32bbfd63` (byte-verified iter 125); reader-visible name inconsistency, not a sync-gate issue (audit substrings use the correct-spelling h5). Trivial canon-first fix (3 lines) for any next content iteration touching Part 9/10 — or fold into ed-3/ed-8. |

---

## Completed tracks (one-liners — detail in worklog/CHANGELOG/git)

- mig-5 Enneagram data build slice (iter 136: §5.4 canonical owner unchanged; `scripts/generate_enneagram.mjs` wired into `pnpm run build`; generated `data/enneagram.json` (schema 2.1.0 — canonical fields §5.4-verbatim, v1 extended variants dropped, LIE folded to one internal copy `types[].lie_template`, spine FLAW/NEED single-source); builder + synthesis re-pointed; vs-e10 unchanged; E10 mini-cards SHARED_REFERENCE (static defaults → §5.4, canon marker ownership declaration); §5.2 6w5 directions locked; `scripts/audit_enneagram_parity.py` PASS (11 checks); map §5.4 EN-1…EN-8; battery green, hash unchanged, baselines exact) — COMPLETE.
- mig-4 Token budget build slice (iter 135: §7A.12 canonical owner unchanged; E01 verified SHARED_REFERENCE — the foundation §4.4 «~100–200 wrong-side copy» claim found absent, registry corrected; E15 Examples total carries the derivation note «(итог: 40–80 на пример × 2–5 примеров — §7A.12)»; canon [VS: E01/E15] markers added; `scripts/audit_token_budget_parity.py` PASS; map §5.3 TB-1…TB-8; R11 row + §7B.2 Greeting «50–100» instance recorded; battery green, hash unchanged, baselines exact) — COMPLETE.
- mig-2 CORE DIRECTIVES build slice (iter 134: §7A.13 verbatim re-print → DEC-08 shorthand canon+master; E08 = the one visual presentation, SHARED_REFERENCE parity-locked by `scripts/audit_core_directives_parity.py`; node-7 title aligned to canonical h4; §7A.1/Part 10 ×4 verified shorthand; map §5.2 D-1…D-8 dispositioned; prototype/extract §6.1-deferred; KI#80 typo fold; battery green, hash unchanged, baselines exact) — COMPLETE.
- mig-1 glossary build slice (iter 133: DEC-18 ratification — §5.1 statuses + 6 ⚑ resolved as identifier heads; v2 build phase opened; `docs/canon/glossary_registry.md` 45 entries + `scripts/generate_glossary.mjs` wired into `pnpm run build` + generated `data/glossary.json` (9.2.6, no core_rules/unified_definition) + runtime/no-JS re-pointed via the data layer + `scripts/audit_glossary_parity.py` PASS; battery green, hash unchanged; KI#72 5th location eliminated) — COMPLETE.
- Docs-restructure plan (iter 1–101) — COMPLETE (agent docs in English since iter 101).
- Canon migration (iter 7–18) — COMPLETE: 10 Parts + 4 Appendices + Part 0 migrated, 97/97 sync.
- Phases A–E content work (iter 81–108) — COMPLETE.
- Multilingual forks A–C (iter 110), Fork D 1/3 voice-hierarchy widget (iter 111) — COMPLETE.
- Dead code/CSS debt (iter 112–115: dead widgets, Mermaid infra, both CSS files) — COMPLETE.
- Agent operating-system rework (iter 116) — COMPLETE (this iteration's predecessor context).
- Documentation hygiene (iter 117: content_map.md + architecture.md content pass, NAV §5 directive fix) — COMPLETE (obs-1/obs-2 closed; KI#67 + obs-3/obs-4 recorded).
- `qa:doc-versions` gate fix (iter 120: KI#67 CLOSED — regex accepts bold + `**Дата:**` headers; 8/8 docs parsed) — COMPLETE.
- Registry C glossary-slice seeding + glossary per-term review (iter 132: DEC-17 owner decisions recorded; 26 matrix rows → C-1…C-26 with back-pointers; 55 JSON terms → T-01…T-55, 35 MERGED / 20 MOVED, 45 unified entries, 6 ⚑ head-form flags; RepPen «1.00–1.10» = 5th KI#72-family location recorded) — COMPLETE (doc-only).
- Editorial matrix Phase A (iter 120: Parts 1/3/7A/9, 210 block rows, repeat registry R01–R20) — COMPLETE.
- Editorial matrix Phase B (iter 126: Part 0/2/4/5/6/7B/8/10 + 4 appendices — 61 sections, 290 rows; full coverage 99 IDs / 500 rows script-verified; registry R01–R29; candidates #14–#20; KI#77 opened; glossary CORE DIRECTIVES classified for ed-2) — COMPLETE.
- ed-8/R18 Phase B canon vague-ref batch (iter 127: 33 refs → specific targets verified against master anchors + 6 part_08 parens + KI#77-a–d; part_05 L21 master mirror + sync-audit P0-16 update; battery green, baselines exact) — COMPLETE.
- ed-8/R18 Phase A remainder + R17 canon-side strips (iter 128: 16 refs (grep recount — part_09 ×12 incl. matrix-unflagged L64/L134, part_07a ×3 + L699 paren, part_03 ×1) → specific targets verified against master anchors; R17 strips #14/#15/#17/#18 — history/repo-meta labels dropped, substance kept; master untouched by design; battery green, baselines exact) — COMPLETE.
- ed-7-family batch + R17 [B] fold (iter 129: matrix candidates #4 R05 + #5 R03 — §3.1 Методология/Pattern-Matcher compressions with master mirrors + sync-audit P3-6/P1-3 updates; R29 terminology ×4 normalized; part_00 L13 + part_08 L27 strips — R17 family complete; KI#78 recorded and deferred) — COMPLETE.
- ed-4-family batch + KI#78 fold (iter 130: candidate #6 R06 — §3.2 Пояснение re-explainer deleted with master mirror, R06 family consolidated; KI#78 CLOSED — part_07b L407 paren-dupe dropped + gloss family aligned to canon L257; KI#79 recorded — drift-tool P_TAG_RE `<pre>` absorption, informational) — COMPLETE.
- Reader/repo-metadata separation ed-6 + KI#71 (iter 121: rendered prose zero `KI#…`/`iter NN`, canon+master, Part 1/7A/8) — COMPLETE.
- Editorial policy adoption ed-policy (iter 122: owner-amended rule set → `AGENTS.md` Editorial Policy + fence #13, canon `_README.md` §4.4, DEC-15) — COMPLETE.
- VS-EMBED E06/E10 defect fixes vs-fix-1 (iter 124: KI#73–76 — E06 two-column restructure + duplication/theme-color removal, E10 hexad/directions/arrowheads, `vs-e10-enneagram.js` data fetch + clamp; runtime suite 30/30 + VLM verified) — COMPLETE.
- Self-admitted dupes cleanup dupes-1 (iter 125: R16 §9.11 `<details>` dupe deleted + dangling-ref repair → §9.3 + E14; R01 §7A.1 full-copy → 1-sentence + refs + E01 viz pointer; battery green, runtime smoke 12/12, obs-6 recorded) — COMPLETE.
