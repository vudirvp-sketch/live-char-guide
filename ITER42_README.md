# iter 42 — COMPONENT-EXTRACTS DRIFT AUDIT

**Build hash:** `69d9b813` (unchanged с iter 34 — KI#23 fix; canon + doc файлы НЕ входят в hash computation)
**Дата:** 2026-07-08
**Task ID:** 42

## Что сделано

1 KI закрыт: **KI#32 ✅ CLOSED (doc-only)** — component-extracts drift audit + HISTORICAL SNAPSHOT notice.

**Pairwise diff audit** выявил ожидаемый drift между `visual-system/integration/component-extracts/` (54 historical snapshot files) и canonical source files:
- 18/18 `E##-visual.html` — DRIFT (KI#13 + KI#22 + structural)
- 16/18 `E##-styles.css` — MATCH vs `vs-styles.css` SECTION 5 (только E15 KI#22, E18 iter 25 с drift)
- 18/18 `E##-script.js` — DRIFT (KI#20 + KI#16)

Файлы НЕ синхронизировались (54 файла, high risk, low value — extracts не используются в build/runtime). Вместо sync: README обновлён с HISTORICAL SNAPSHOT notice + 2 новых audit scripts как regression tests.

## Изменённые файлы (8 total)

**Content files (1):**
- `visual-system/integration/component-extracts/README.md` — HISTORICAL SNAPSHOT notice, drift table, regeneration instructions

**New audit scripts (2):**
- `scripts/audit_component_extracts.py` (211 строк) — visual.html vs VS-EMBED block в master
- `scripts/audit_component_extracts_css.py` (135 строк) — styles.css vs SECTION 5 в vs-styles.css

**Doc files (4):**
- `STATUS.md` — iter 42 record + KI#32 section + Invariants + Roadmap + cleanup (KI#25-29 trimmed to one-liners)
- `worklog.md` — iter 42 record (самый подробный), iter 41 → one-liner
- `AGENT_NAVIGATION.md` — header + §8 OP-1 iter 42 row + iter 43+ roadmap + «Подсказка следующему агенту»
- `docs/AUDIT_VERIFICATION.md` — §5.8 iter 42 section

**Build artifact (1):**
- `index.html` — build timestamp updated (auto-regenerated by `pnpm run build`)

## Validation gates — ALL PASS

- `build` — hash `69d9b813` unchanged
- `validate:master` (12 checks) PASS
- `validate` (8 gates, index.html 7.5KB) PASS
- `test:unit` 43/43 PASS
- `test:integration` 21/21 PASS
- `lint` 0 errors, 12 warnings (baseline) PASS
- `qa:csp` PASS
- `qa:bundle` 7.5KB PASS
- `qa:doc-versions` PASS
- `audit_vs_embeds.py` 0 regressions PASS
- `audit_component_extracts.py` runs (18/18 DRIFT expected) PASS
- `audit_component_extracts_css.py` runs (16/18 MATCH + 2 DRIFT expected) PASS
- `check_english.py --scan-docs` 0 WH40k terms PASS

## Установка

Распаковать поверх локальной копии репозитория с сохранением структуры папок:

```bash
# Из корня репозитория live-char-guide:
unzip -o iter42-live-char-guide.zip
```

## Git commands

```bash
cd /path/to/live-char-guide
git add -A
git commit -m "iter 42: KI#32 CLOSED - component-extracts drift audit + HISTORICAL SNAPSHOT notice

Pairwise diff audit (scripts/audit_component_extracts.py + audit_component_extracts_css.py, both new) revealed expected drift between visual-system/integration/component-extracts/ (54 historical snapshot files) and canonical source files:
- 18/18 E##-visual.html - DRIFT (KI#13 + KI#22 + structural)
- 16/18 E##-styles.css - MATCH vs vs-styles.css SECTION 5 (only E15 KI#22, E18 iter 25 with drift)
- 18/18 E##-script.js - DRIFT (KI#20 + KI#16)

Files NOT synced (54 files, high risk, low value - extracts not used in build/runtime). Instead: component-extracts/README.md rewritten with HISTORICAL SNAPSHOT notice + canonical source pointers + drift table + regeneration instructions.

Modified files (8): STATUS.md, worklog.md, AGENT_NAVIGATION.md, docs/AUDIT_VERIFICATION.md, visual-system/integration/component-extracts/README.md, scripts/audit_component_extracts.py (new), scripts/audit_component_extracts_css.py (new), index.html (build timestamp).

All validation gates PASS: build (hash 69d9b813 unchanged) / validate:master (12 checks) / validate (8 gates) / test:unit (43/43) / test:integration (21/21) / lint (0 errors) / qa:csp / qa:bundle / qa:doc-versions / audit_vs_embeds.py (0 regressions) / audit_component_extracts*.py (expected drift confirmed) / check_english.py (0 WH40k terms)."
git push origin main
```

## Точка остановки

iter 42 полностью завершён. KI#32 CLOSED. Архив загружен, git-команды подготовлены.

**iter 43+ Roadmap (deferred, не критично):**
- **Glossary double-render inefficiency** - LOW, structural by design
- **Component extracts regeneration (опционально)** - LOW, нет business value пока extracts не используются

**Invariants (iter 42+):** see STATUS.md § Invariants

**При обнаружении новых багов — сначала документировать в STATUS.md как KI#N, потом фиксить.**

**Для старта iter 43:** прочитай STATUS.md (iter 42 record), worklog.md (iter 42 = самый подробный), AGENT_NAVIGATION.md (§8 OP-1 iter 42 row + iter 43+ roadmap + «Подсказка следующему агенту» в конце). Build hash baseline `69d9b813`.
