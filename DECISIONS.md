# DECISIONS.md — Standing Decisions

> Append-only. Each entry: decision → why → consequence. Supersede with a new entry,
> never delete. Cap ~20 rows — over cap, merge related rows into compound IDs (citations
> must keep resolving). This is the long-term home of **why**; `worklog.md` records **what**.
> IDs here are `DEC-NN`. (Content-duplicate IDs `D1–D20` in `docs/research/` are a
> different namespace — do not collide.)

| ID | Date | Decision | Why | Consequence |
|----|------|----------|-----|-------------|
| DEC-01 | 2026-08-13 | Repo language law: agent-facing docs/commits in English; guide content bilingual per content rules; owner chat in Russian | consistent LLM consumption (iter-101 rewrite); content language is a product property | new agent docs written in English; never translate the guide itself via agent initiative |
| DEC-02 | 2026-06-23 | Canon (`docs/canon/part_NN.md`) = single source of truth; `src/master/*.html` derived | duplicated semantics drifted 3–5× per concept (KI#14 era) | on drift, canon wins; master reconciles; `audit_canon_master_sync.py` MUST pass |
| DEC-03 | iter 14 | `viz > dry text`: visualization = replacement, not addition | parallel text+visual duplication bloats tokens and drifts | if a VS-EMBED shows a concept, text must not re-explain it; unique visualizations are not deleted |
| DEC-04 | v8 | Root fallbacks (`parts/`, `widgets/`, `assets/`, `event-bus.js`, `index.html`, `build.hash`) are committed but regenerated | GitHub Pages backward-compat without CI on every push | NEVER hand-edit root fallbacks; edits go to `src/` + `data/`, then `pnpm run build` |
| DEC-05 | 2026-09-13 | `build.hash` = first 8 hex of `sha256(src/shell/index.html)` — cache-busting value only | verified in `src/scripts/build-shell-unified.mjs`; content/CSS/data changes must not false-positive the hash | hash unchanged ≠ nothing changed; diff vs BASE_COMMIT is the real delivery check (AGENTS.md delivery discipline) |
| DEC-06 | 2026-08-14 | Mermaid removed entirely; VS-EMBEDs own all diagrams; CSP carries no CDN script-src | content had zero `.mermaid` usages since iter 14; CDN = unused attack surface | no diagram libraries; new diagrams = VS-EMBED elements per `docs/components.md` |
| DEC-07 | 2026-08-13 | Theme model: OLED/dark default (no class) + Light (`body.theme-light`) only | dark class was dead complexity (iter 98/99) | no third theme; theme CSS changes must check both modes |
| DEC-08 | iter 93 | `{{CORE_DIRECTIVES — ...}}` shorthand is the accepted navigational convention (D4) | one definition — one place | shorthand references Part 7A; never expand the directive list inline |
| DEC-09 | iter 111 | Widget data lives in `data/*.json`, never hardcoded in JS; single exception: `persona-voice-hierarchy` embeds canon-constant prose values | data/JSON keeps widgets data-driven and auditable; §3.2 table values are canonical prose, not widget data | new widgets default to `data/*.json`; exceptions must cite canon provenance in the widget header |
| DEC-10 | 2026-09-13 | Agent-doc split of concerns: `AGENTS.md`=law · `AGENT_NAVIGATION.md`=current-state map · `STATUS.md`=state+Next step · `PLAN.md`=backlog · `DECISIONS.md`=why · `worklog.md`=history | state fragmentation and duplicated roadmap/iteration numbers caused drift (README stuck at iter 101; PLAN stale; NAV carried history) | one fact — one owner (NAV §10); iteration number lives ONLY in `STATUS.md`; navigation never carries history |
| DEC-11 | 2026-08-13 | Doc-only commits may skip artifact build: `SKIP_ARTIFACT_BUILD=1` (lint still runs) | builds churn root `index.html` timestamp for zero functional change | only for commits not touching `src/` or `data/`; exploratory builds' timestamp noise gets restored, not committed |
| DEC-12 | 2026-08-13 | KI lifecycle: record → fix-if-in-scope → close → delete after 2 iterations | unrecorded bugs recur (KI#63/64 same version-drift pattern) | discovering a problem never grants permission to fix it (scope discipline, AGENTS.md) |
| DEC-13 | 2026-09-13 | QA gates judged individually with fixed baselines: english=19 by design, syntax=247 pre-existing | aggregate `pnpm run qa` exits 1 by design — the exit code is not the signal | counts must not increase; PASS/FAIL only for csp/bundle/contrast/doc-versions; new leaks = regression |
| DEC-14 | 2026-09-14 | Part-fetch resilience pattern (KI#69): per-part fetch gets retry (2×, backoff 300/900 ms; network/5xx/429 only, 4xx fail fast) + in-place `role=alert` placeholder + surgical single-part re-fetch re-running the standard init sequence; runtime-UI classes (`.load-error`, `.load-retry-btn`) live in shell CSS, outside the `docs/components.md` content registry | transient network failures self-heal, persistent ones become visible at the exact gap with one-click recovery; alternatives (log-only / retry-only / banner-only) each leave a failure class untreated; architectural bundling rejected as out of research scope | new fetch-into-DOM flows (e.g. manifest-driven content) must reuse `fetchPartHtml()` + placeholder machinery instead of bare `fetch().catch()`; content-facing error markup stays Russian, code/comments English |
| DEC-15 | 2026-09-14 | **Editorial Policy adopted as content-editing law** — owner-amended rule set (research §27 core + §22 functional-load test expanded to 5 checks + §24 UNCLEAR classification + §21 structured-representation preservation): compress redundant presentation, never unique capability; success = reduced redundant semantic load + navigability, never word count. Full text: `AGENTS.md` → Editorial Policy (cited once; canon-side view `docs/canon/_README.md` §4.4) | owner reviewed the research rule set and accepted it **with amendments** (chat, 2026-09-14); the amendments explicitly resolve the conflicts vs existing law: functional repetition preserved when it serves a distinct function (subsumes the useful-repetition whitelist: Price / SPINE causality / Show Never Tell / Embodiment), compression-as-replacement stays consistent with `viz > dry text` (DEC-03), cross-ref replacement rules stay consistent with IMP-48 (fence #11) | all content edits in `docs/canon/` + `src/master/` must pass the 5-point check (capability / coverage / retrieval / context / dependency) before any deletion/merge/move/cross-ref replacement; uncertain cases = UNCLEAR, never deleted by assumption; `ed-1`…`ed-8` + `dupes-1` execute under it; policy text amendable only by owner decision |
| DEC-16 | 2026-09-14 | **Two-layer guide language policy (iter-123 russification)** — Russian is the default guide language: all prose, headings, and callout labels (`ПРАВИЛО`, `РЕКОМЕНДАЦИЯ`, `ПРИМЕР`, `ИЛЛЮСТРАЦИЯ`, `ШАБЛОН`, `Переход`, `Синтез`, `Ссылка`, `Демонстрирует`) are Russian; ordinary English terminology is translated (`token → токен`, `lie → ложь`). English stays ONLY in: (a) executable/recommended prompt content prescribed in English (SP blocks, `<CORE_DIRECTIVES>` wording, Tone Frame strings, card examples, copy-paste templates) — each block keeps an adjacent Russian explanation; (b) technical identifiers (section IDs, XML tags, class names, machine-readable values); (c) proper names. Supersedes the pre-iter-123 "English semantic anchors" rule (callout labels and ordinary terms in English) while DEC-01's agent-docs-in-English stands | owner explicitly requested full russification in chat (2026-09-14): "все русифицировать что можно"; the old English-anchor rule made reader prose artificially English and hurt readability for the Russian-speaking audience; the three-way classification (executable content / prose / identifier) protects prompt engineering correctness — canonical English directive wording inside executable prompts is untouched | every future content edit follows fence #12 (two-layer); `qa:english` baseline re-baselined 19 → 18 (one prose leak legitimately translated; the remaining 18 are executable-content leaks by design); `audit_canon_master_sync.py` expectations updated to Russian labels; new callout labels must be Russian with the English form never restored |
| DEC-17 | 2026-09-14 | **Glossary cluster v2 architecture (first bounded area, mig-1):** (a) canonical term-set = **merged-with-review** — one registry over the union of the v1 sets (25 canon entries + 55 JSON terms, 21 overlap → 45 unified entries); canonicality is decided **per term** — JSON-only terms do not become canonical merely by existing in the machine layer (per-term evidence + proposed dispositions: `migration_map_v2.md` §5.1); registry presentation is Russian-first per DEC-16 — natural direct translations replace English head-terms (`lie → ложь` immediately, `token → токен`), English survives only as technical identifiers (XML/API/parameter names, SP-element names, protocol names, framework acronyms), boundary cases flagged for owner review; (b) source-of-truth chain = **canonical term record → generated `glossary.json` → runtime panel / no-JS glossary** (machine layer becomes derived, generated — never hand-edited after the switch); (c) glossary stays the **v2 Reference-layer** representation of the unified registry (appendix layer rides the v2 Reference-mode design; the v1 KI#70 wire/drop question stays deferred/superseded) | owner chat decisions 2026-09-14 (answers to `migration_map_v2.md` §6.3 a–c): the three competing v1 term sets drift (21 divergent overlap definitions incl. stale GHOST Layers framing, version 9.2.0 stale, dead `core_rules`); composition and source direction needed an owner call — the unification mechanism itself is architectural | mig-1 unblocked (gate cleared); §5.1 statuses/head-forms stay PROPOSED until the owner ratifies them and the build slice executes; `data/glossary.json` becomes a generated artifact; dead `core_rules` → `REMOVED_WITH_REASON`; Registry C gets a slice per Part at each build-slice time (glossary slice seeded iter 132) |
| DEC-18 | 2026-09-14 | **mig-1 ratification + v2 build phase open + registry location (glossary slice executed iter 133):** (a) owner ratified `migration_map_v2.md` §5.1 per-term dispositions — 35 MERGED / 20 MOVED / 45 unified entries — and resolved the 6 ⚑ head-forms: all six keep English identifier heads with adjacent Russian gloss («System Prompt / SP (системный промпт)», «Author's Note (AN)», «Description (блок описания)», «Examples (примеры диалогов)», «Format Lock (фиксация формата)», «Tone Frame (тональный фрейм)»); the owner's control check (no ⚑ term violating "English only as technical identifier" in the v2 prose) is enforced by `scripts/audit_glossary_parity.py` (english-rule check); (b) the **v2 build phase is open** (owner: «можешь продолжать работы по плану») — the documentation-loop alarm is satisfied by the mig-1 functional slice; (c) canonical term registry location = **`docs/canon/glossary_registry.md`** (canon-side structured source, DEC-02/L1 continuity; entry pattern relocated per DEC-17c); the machine layer `data/glossary.json` becomes **generated** (`scripts/generate_glossary.mjs`, first stage of `pnpm run build`) | owner chat 2026-09-14 (§5.1 review: «карта миграции сейчас выглядит здоровой; 6 ⚑ я бы не переписывал» + «убедиться, что 45-entry unified registry реально реализуется именно по цепочке canonical term record → generated glossary.json → runtime/no-JS» + go-ahead); DEC-17 explicitly awaited ratification before execution; the canon-side location keeps content source-of-truth in `docs/canon/` — the same derivation direction map §2 L6 sets for other knowledge-carrying data files (e.g. enneagram → §5.4) | `data/glossary.json` is a build artifact — hand-edits forbidden, term changes go through the registry only (drift shows in `git status` on every build); runtime panel + no-JS page consume the generated unified registry (re-pointed via the data layer — the delivery path already read `data/glossary.json`, zero shell code changes); `appendix_glossary.md` stays frozen v1 migration source until the v2 switch disposes it; dead layers dropped (`core_rules`, `unified_definition` → REMOVED_WITH_REASON); version field generated from `src/VERSION` (stale 9.2.0 fixed); RepPen value removed from the machine layer (KI#72-family 5th location eliminated) |
| DEC-19 | 2026-09-14 | **Visual-markup ownership + visual-system layer disposition (map §6.1 + §6.4):** master embed (L2) = the canonical visual markup; `visual-system/elements/` prototypes (L4) + `integration/component-extracts/` (L5) = **frozen design artifacts** — archive = git history + recorded reasons; per-family stale copies are disposed as `REMOVED_WITH_REASON` **at their slice time** (the four executed families E08/E01/E15/E10 disposed iter 138; E05/E06/E09… ride the same freeze at their own slice time). Hero (`visual-system/hero/`) = remove/archive, never integrate — §6.4 reduces to the disposal slice + the KI#81 dead-CSS cleanup | owner chat 2026-09-14 «начинай работу по планам» — the go-ahead on the iter-137 recommended package ([`owner_gates_iter137.md`](./docs/research/owner_gates_iter137.md) §3.1/§3.4), the only prepared plan pending a call; production already works this way (prototype↔embed identity 93% = 494/532, foundation §4.2; live drift E10 3-way + E01/E05/E06 russification lag; no L4/L5 sync tooling — foundation §4.3 INFERENCE: master embeds are the de-facto production canonical); keeping prototypes as a second canon = consciously preserving a future-drift source; hero would reintroduce the external-CDN script-dependency class iter-113 removed (Three.js via `cdn.jsdelivr.net` importmap) with zero consuming requirement | target model map §2: canon owns knowledge → master owns visual implementation → root fallbacks generated; future migration slices stop making per-slice prototype-sync decisions; hero disposal slice pending (remove/archive + `.vs-hero-placeholder` CSS stub cleanup = KI#81 rides it); production `Source: elements/E##-*.html` provenance comments in `src/master/` + stylesheets resolve to git history after disposal (frozen v1 — comments untouched) |
| DEC-20 | 2026-09-14 | **E13 diagnostics disposition + rule-strength convention (map §6.2 + §6.5):** (a) E13 = `TEXTUAL_CANONICAL` — the unique symptom→check→branch→AP/E mappings canonicalize in canon §9.6; E13 stays the visual presentation / decision aid of that tree (`VISUAL_CANONICAL` NOT declared); (b) rule-strength vocabulary ratified: **RULE / GUIDELINE / OBSERVATION / EXPERIMENTAL** + UNKNOWN = temporary uncertainty marker only (never a final class; never deleted while UNKNOWN); applied **incrementally per slice** — no mass annotation of v1 or v2; ПРАВИЛО (`.callout.rule`) ↔ RULE, РЕКОМЕНДАЦИЯ (`.callout.rec`) ↔ GUIDELINE; OBSERVATION/EXPERIMENTAL markup representation = the first consuming slice (new components need fence #7 infrastructure approval) | owner chat 2026-09-14 «начинай работу по планам» — the go-ahead on the iter-137 recommended package ([`owner_gates_iter137.md`](./docs/research/owner_gates_iter137.md) §3.2/§3.3); canon §9.6 L139 already claims E13 is «визуальная версия дерева для 3 базовых симптомов» — currently false (the mapping content is not a subset of the §9.6 table); TEXTUAL_CANONICAL makes the existing canon claim true; diagnostic knowledge locked inside a visual embed is worse for search, testing, authority — the Debug reader mode needs a textual canonical home; the strength vocabulary was already owner-directed (foundation §8.4) — this call is formal ratification + semantics + application scope (research §19 four-class mapping: Rule → RULE · Recommended target → GUIDELINE · Observed heuristic → OBSERVATION · Diagnostic threshold → per case) | **mig-3 unblocked** (Diagnostics cluster / Debug mode — the last Registry B bounded area with a ready plan) on the established slice pattern: canon §9.6 mappings (Editorial Policy 5-point check — E13 keeps its visual function) → master mirror → E13 re-pointed to the canonical mapping → new `audit_diagnostics_parity.py` + Debug reader-path audit; Part 7A/8 v2 slices get the strength convention — the sampling cluster additionally waits on the separate KI#72 value decision; model-capability percentages (`appendix_model_table.md`) = first known OBSERVATION-strength applications |
| DEC-21 | 2026-09-15 | **Voice influence % cluster disposition (map §6 row 6 / Registry B row 7 — the last unexecuted bounded area):** (a) V-a — E07 scale/table = `SHARED_REFERENCE`: the §3.2 6×3 table stays the single value owner; compressed scale, split axis and the red 0% marker are visual encodings, not values; (b) V-b — E07 unique prose = `TEXTUAL_CANONICAL`: canonicalized in the §3.2 «Хранилище ≠ Влияние» prose block (Хранилище vs Влияние distinction + SP-primacy misconception + drift implication); `VISUAL_CANONICAL` NOT declared; (c) V-c — widget notes split: the two quantitative claims (~10–20 сообщений; ~5 реплик) canonicalize into §3.2; the four rule restatements stay widget-local operational reminders (DEC-09 unchanged); data/*.json extraction rejected; (d) V-d — **Option A** (table wins): §3.2 AN 12B stays ~2%; the two «не влияет» statements (§7A.5, §7A.7) + the widget note re-frame to «влияние пренебрежимо мало (~2%)»; no numeric change anywhere; E16 badge gains the derivation note «диапазон = мин–макс строки AN — §3.2» (E15 pattern); (e) V-e — row-label set: row 1 = «Недавний чат» (canon + master + widget/export; embed label stays as-is), five card-field names unchanged | owner chat 2026-09-15 «продолжай работу по плану!» — the go-ahead on the iter-141 recommended package ([`voice_cluster_iter141.md`](./docs/research/voice_cluster_iter141.md) §4; same go-ahead form as DEC-18 «можешь продолжать работы по плану» / DEC-19+20 «начинай работу по планам» — the only prepared plan pending a call); the numeric core was verified in green parity at every layer (widget 6/6 == canon — no KI#72-style contradiction), so the calls are architectural (ownership declaration + prose canonicalization); Option A is the honest framing on OBSERVATION-strength percentages (0% carries RULE semantics in this table — Description «0% ЗАПРЕЩЕНО» — which a hard AN 0% would dilute) and the cheaper call (no 3-layer numeric change) | the voice-cluster build slice executed immediately (iter 142, mig-2/mig-3 pattern): canon §3.2 canonicalization + full `[VS: E07]` marker → §7A re-frames → master mirrors + E07/E16 re-point → widget header/notes → `scripts/audit_voice_parity.py` (new MUST-PASS parity gate) → KI#82 CLOSED → E07 prototype/extract disposal (DEC-19); Registry B is now fully dispositioned except sampling values (KI#72 owner call — the last bounded area) |
| DEC-22 | 2026-09-15 | **Sampling values cluster disposition (map Registry B row 2 / KI#72 — the last bounded area of semantic extraction; gate package [`owner_gates_iter144.md`](./docs/research/owner_gates_iter144.md) §2, Option A ratified as the S-a…S-d package):** (a) S-a — §7A.6 = the single canonical sampling value owner: the «Базовые параметры» table stays authoritative (32B+/API Temperature **0.7–1.0**, RepPen **1.05–1.10**; point recs via «Модель-специфичные»: 32B+ 0.7–0.9 / 1.05); AP-5 boundary «RepPen ≤ 1.10» and AP-7 (PP = 0.0) unchanged — Phase B verified both hold under the call; (b) S-b — §7A.7 param rows (Temperature / Presence Penalty / RepPen) dropped from the canon table, header «Параметр» → «Возможность», defer line added; the five capability rows stay (canon catches up to the deployed master shape); (c) S-c — E17 = `SHARED_REFERENCE` visual presentation: the 32B+ column shows the canonical «32B+/API» values — no «базовая/чеклист» dual-side split, no orphan asterisk; the 12B–32B middle tier = **declared omission** (values live in the §7A.6 canonical table only — no embed representation before or after the slice, now declared in the embed comment + canon marker); (d) S-d — hint re-frames: E12 AP-5 fix card («Держите RepPen в диапазоне своей модели — §7A.6 (никогда > 1.10)»), §9.3 parenthetical (12B-qualified), §9.4 problem #4 (model-range defer), §9.10 «Повторы фраз» row (12B-qualified + §7A.6 ref); glossary already defers (5th location eliminated iter 133 stays eliminated) | owner chat 2026-09-15 «я согласен с рекомендациями, сделай как лучше и качественнее» — the go-ahead on the iter-144 package §2/§4 (G1 recommendation; same single-reply form as iter-137 → DEC-19+20): §7A.6 is internally coherent (both its tables + PP rules + the AP-5 note orbit the same values while §7A.7 is a summary that drifted); the repo's cross-reference mesh already treats §7A.6 as canonical (6 deferring refs vs 0); it is the recorded matrix/PLAN/Registry B recommendation (R12); cheapest correct edit — §7A.7's param rows are canon-only (master already points at E17); Options B (§7A.7 canonical) and C (union envelope) rejected with reasons in the package §2.5 | sampling-cluster build slice executed iter 145 (map §5.7 SP-1..SP-12, mig-2/mig-3/voice pattern): canon §7A.6 full `[VS: E17]` marker + §7A.7 capability-only + defer → §9.x/E12 re-frames → master mirrors + E17/E12 re-points → new MUST-PASS gate `scripts/audit_sampling_parity.py` (parity ×7) → KI#72 CLOSED → E17/E12 prototype/extract disposal (DEC-19, 8 files); Appendix B model-capability percentages = the first OBSERVATION-strength application (DEC-20, rides this slice); stated consequence: the «0.85–1.1» Temperature side disappears from the live site — the canonical top for 32B+ becomes 1.0; semantic extraction **COMPLETE (7/7)** |
| DEC-23 | 2026-09-15 | **v2 architecture phase OPENED + first slice = P-a consolidated specification** (map Registry B / STATUS Next-step row 2; migration order: semantic extraction COMPLETE → **v2 architecture** → v2 build → parity audit → canonical audit → reader-path audit → switch; reader modes Learn → Build → Debug → Reference): the phase's first bounded slice = one design document `docs/research/v2_architecture.md` consolidating the verified in-repo inputs — the layer model (map §2 target), the reader-mode specification (F1: per mode — entry point, default visibility, on-demand disclosure), the presentation-disposition policy (F2 vocabulary on the matrix / Registry C), checklist triage (F3), example tiering (F4), the Part micro-template (F5), the disclosure-component policy (F6: `<details class="interactive">`), the rule-strength markup convention (DEC-20 application — first application executed iter 145), the cluster/parity inventory (7 clusters + gates, now complete), and the switch plan (audit stages → switch criteria); the spec is PROPOSED — the owner ratifies it before any v2 build slice executes | same owner message as DEC-22 (single reply decides both — the iter-137 → DEC-19+20 precedent); package §3: every input is already verified and sitting in the repo (foundation §6–§8 + map §2/Registry A-B + intake F1–F6 + DEC-17..22) — the spec is consolidation, not new research; P-b (reader-mode spec only) defers decisions the phase exists to make; P-c (vertical pilot) risks building on an unspecified architecture against the track's own bounded-area discipline (design → ratify → build); recommended order G1-first executed: the spec starts from a complete semantic inventory with no sampling-shaped hole | the spec slice executes as the next iteration (iter 146, doc-only — no v1/v2 production file touched); ratification of the spec → DEC-NN entries for the ratified items; no v2 build slice before that ratification; phase scope beyond P-a stays owner-called (P-a's own §"open questions" list the ratification decision points) |

Append rule: a new standing decision gets the next `DEC-NN` ID and a one-line worklog
mention; never edit existing rows in place — supersede.
