# iter 49 — Merge Instructions

## Файлы в архиве (modified/new — скопировать с заменой)

- `STATUS.md` — обновлён (iter 49 recon record, KI#34/KI#35 confirmed still open)
- `AGENT_NAVIGATION.md` — обновлён (header iter line + §8 OP-1 iter 49 row + iter 50+ roadmap + «Подсказка следующему агенту» updated)
- `worklog.md` — обновлён (iter 49 detailed record + iter 48 → one-liner)
- `ITER49_README.md` — NEW (iter 49 stopping point + git commands + recon summary)

## Файлы для удаления (deleted в iter 49)

- `ITER48_README.md` — superseded by `ITER49_README.md` (per established iter pattern)
- `DELETES.txt` — устаревший marker-файл (9 строк), ссылался на `ITER47_README.md`, который был удалён ещё в iter 48. Сам `DELETES.txt` — одноразовый marker, iter 48 уже закоммичен → файл устарел → удалить.

## Файлы БЕЗ изменений (НЕ включены в архив)

- `src/master/*.html` — без правок (pure recon)
- `docs/canon/*.md` — без правок
- `src/shell/*` — без правок
- `data/*.json` — без правок
- `scripts/*.mjs` / `scripts/*.py` — без правок
- `package.json` / `pnpm-lock.yaml` — без правок
- `index.html` (root fallback) — regenerated при `pnpm run build`, изменился только `Generated:` timestamp (content identical). В архиве НЕ включён — будет regenerated локально.

## Слияние с локальной директорией

```bash
# 1. Распаковать архив
unzip -o iter_49_recon.zip -d /tmp/iter49

# 2. Скопировать изменённые файлы
cp /tmp/iter49/STATUS.md /path/to/live-char-guide/
cp /tmp/iter49/AGENT_NAVIGATION.md /path/to/live-char-guide/
cp /tmp/iter49/worklog.md /path/to/live-char-guide/
cp /tmp/iter49/ITER49_README.md /path/to/live-char-guide/

# 3. Удалить устаревшие файлы
rm -f /path/to/live-char-guide/ITER48_README.md
rm -f /path/to/live-char-guide/DELETES.txt

# 4. Верифицировать baselines
cd /path/to/live-char-guide
pnpm install --frozen-lockfile
pnpm run build              # Должно вывести: Hash: 69d9b813 (shell hash unchanged)
cat build/build-manifest.json  # contentHash: sha256:84d69ecffca28cbf
```

См. полный git workflow в `ITER49_README.md`.
