# iter 34 — CSS Callout Scoping + CSP worker-src Fix

> **Build hash:** `fd3d96d3` → `69d9b813` (изменён из-за KI#23 fix в `src/shell/index.html`)
> **Дата:** 2026-07-08
> **Closed:** KI#22 ✅, KI#23 ✅, KI#24 ✅ VERIFIED (no bug)
> **Validation gates:** ALL PASS (validate:master 12/12, validate 8/8, test:unit 43/43, test:integration 21/21, lint 0 errors / 12 warnings, qa:csp PASS, qa:bundle 7.5KB, qa:doc-versions PASS, audit_vs_embeds.py 0 regressions)

---

## Содержимое архива (11 файлов)

### Canonical sources (редактировать тут — все правки в `src/`)
| Файл | Описание | Изменения |
|------|----------|-----------|
| `src/shell/styles.css` | Shell стили (включая E15 ELEMENT STYLES section) | **KI#22 fix:** 7 selectors scoped — `.callout` → `.blueprint-area .callout` (line 6278), `.callout.is-visible` → `.blueprint-area .callout.is-visible` (6292), `.callout--structure/anchors/spine/directives` → `.blueprint-area .callout--*` (6297/6302/6307/6312), `.callout-line` → `.blueprint-area .callout-line` (6318). + explanatory comment block. |
| `src/shell/index.html` | Shell HTML (CSP meta, FAB buttons, panels) | **KI#23 fix:** `worker-src 'self' blob:;` added к CSP meta (line 22). + 3-line explanatory comment. |
| `src/assets/vs-styles.css` | Visual System стили (E01-E18 element extracts) | **KI#22 fix:** те же 7 selectors scoped — `.callout*` → `.blueprint-area .callout*` (lines 2873, 2887, 2892, 2897, 2902, 2907, 2913). + explanatory comment. |

### Root fallbacks (regenerated by `pnpm run build` — копии canonical sources)
| Файл | Источник | Зачем включён |
|------|----------|---------------|
| `assets/shell-styles.css` | copy of `src/shell/styles.css` | Root fallback для GitHub Pages backward compat |
| `assets/vs-styles.css` | copy of `src/assets/vs-styles.css` | Root fallback |
| `index.html` | regenerated from `src/shell/index.html` | Root fallback (с build hash в comment) |
| `build.hash` | `69d9b813` (new) | Build hash file |

### Documentation
| Файл | Изменения |
|------|-----------|
| `STATUS.md` | iter 34 record + KI#22 ✅ CLOSED + KI#23 ✅ CLOSED + KI#24 ✅ VERIFIED. KI#21 fix plan shifted iter 34-37 → iter 35-38. |
| `worklog.md` | iter 34 = самый подробный record; iter 33 → one-liner. |
| `AGENT_NAVIGATION.md` | Header iter line + §6 pitfalls #41 (KI#22) + #42 (KI#23) + §8 OP-1 iter 34 row + iter 35+ roadmap + §10 hint updated. |
| `docs/AUDIT_VERIFICATION.md` | Build hash baseline `fd3d96d3` → `69d9b813`. §4 iter numbering shifted 34→35, 35→36, 36→37, 37→38. §5 stop points + §6.1 + §7 updated. |

---

## Как мерджить с локальной директорией

```bash
# Из корня локальной копии live-char-guide:
unzip iter_34_css_csp_fix.zip -d /tmp/iter_34_merge
cp -r /tmp/iter_34_merge/* .
# Или пофайлово — структура папок в архиве совпадает с репозиторием
```

После мерджа — запустить `pnpm run build` для верификации root fallbacks.

---

## Git-команды для обновления репозитория

```bash
cd /path/to/live-char-guide

# 1. Stage changes (canonical sources + root fallbacks + docs)
git add src/shell/styles.css src/shell/index.html src/assets/vs-styles.css
git add assets/shell-styles.css assets/vs-styles.css index.html build.hash
git add STATUS.md worklog.md AGENT_NAVIGATION.md docs/AUDIT_VERIFICATION.md

# 2. Verify staged
git status
git diff --cached --stat

# 3. Commit
git commit -m "iter 34: KI#22 ✅ CLOSED (Callout CSS Scoping Bug) + KI#23 ✅ CLOSED (CSP worker-src) + KI#24 ✅ VERIFIED (FAB Glossary/TOC)

KI#22 (HIGH): .callout selector was defined TWICE globally — src/shell/styles.css
line 419 (documentation rule/rec/ex taxonomy) AND line 6278 (E15 Annotated Blueprint
annotation labels, NOT scoped — position:absolute; opacity:0; font-size:11px).
Same dual definition in src/assets/vs-styles.css line 2873. E15 (later in file)
OVERRIDES documentation — all 56 .callout rule/rec/ex in 12 master HTML became
absolute-positioned, 11px font, opacity:0 (visible only after .is-visible class
added by scroll observer in iter 32 KI#20 fix, but still overlapping content).
Fix: scope E15 .callout* selectors to .blueprint-area .callout* in both CSS
files (7 selectors per file = 14 total). 56 documentation callouts now use ONLY
line 419 definition (visible by default, proper padding/border). 11 E15
annotation labels (inside .blueprint-area) preserved.

KI#23 (MEDIUM): CSP meta in src/shell/index.html had no worker-src directive.
Fallback to script-src, which doesn't allow blob:. Mermaid v11 (CDN) internally
creates Web Worker from blob URL for parsing — blocked, Mermaid degraded to
single-threaded mode. Fix: add 'worker-src \'self\' blob:;' to CSP meta.
blob: allows only worker creation (not arbitrary script execution) — safe.

KI#24 (LOW, NO BUG): FAB Glossary (#fab-glossary) and FAB TOC (#fab-toc)
verified working. JS handlers wired in initGlossary() (lazy-loader.js line 1208)
and initPanels() (line 1501). Glossary renders 53 terms, TOC panel initializes.
Minor inefficiency: glossary renders twice (init + content loader) — not fixed
in this iter, not critical.

Build hash: fd3d96d3 → 69d9b813 (KI#23 fix modifies src/shell/index.html;
hash computed from this file. KI#22 fix only CSS — doesn't affect hash).

Validation gates ALL PASS:
- validate:master (12 checks)
- build (hash 69d9b813)
- validate (8 gates, index.html 7.5KB)
- test:unit (43/43)
- test:integration (21/21)
- lint (0 errors, 12 warnings)
- qa:csp (no inline scripts, no eval)
- qa:bundle (7.5KB, max 500KB)
- qa:doc-versions (all current)
- audit_vs_embeds.py (0 regressions)

KI#21 (Content Audit contradictions) fix plan shifted iter 34-37 → iter 35-38
(canonical audit delayed by iter 34 = CSS/CSP fix). See docs/AUDIT_VERIFICATION.md
§4 for full plan (16 P0 + 11 P1 + 18 P2 + 12 P3 = 57 canon edits)."

# 4. Push to remote
git push origin main
# (или git push origin master — проверить текущую ветку через git branch)
```

---

## Точка остановки для продолжения в новом чате

**iter 34 — COMPLETE.** KI#22 ✅ CLOSED (Callout CSS Scoping Bug), KI#23 ✅ CLOSED (CSP worker-src missing), KI#24 ✅ VERIFIED (FAB Glossary/TOC — no bug). Build hash `fd3d96d3` → `69d9b813`.

**iter 35+ — что делать в следующем чате:**

1. Прочитать `STATUS.md` (iter 34 record — KI#22/#23 ✅ CLOSED, build hash `69d9b813`; iter 33 one-paragraph — Content Audit Verified без правок кода; KI#21 🟡 DOCUMENTED — Content Audit contradictions, fix plan ready для iter 35-38; iter 32 one-liner — KI#20 ✅ CLOSED; все previous KI#1..#23 ✅ CLOSED/VERIFIED).
2. Прочитать `worklog.md` (iter 34 record — самый подробный; iter 33 one-liner).
3. Прочитать `AGENT_NAVIGATION.md` §6 pitfalls #41 (KI#22) + #42 (KI#23) ✅ CLOSED, §8 OP-1 iter 34 row + iter 35+ roadmap с KI#21 fix plan.
4. Прочитать **`docs/AUDIT_VERIFICATION.md`** (главный deliverable iter 33 — §4 фикс-план: 16 P0 + 11 P1 + 18 P2 + 12 P3 = 57 правок, exact file+line+before/after; iter numbering shifted 34→35, 35→36, 36→37, 37→38).

**Если пользователь даст новую задачу — начать с неё.**
**Если найден новый баг — сначала документировать в `STATUS.md` как KI#N, потом фиксить.**
**VS scroll-animation invariant:** `python3 scripts/audit_vs_embeds.py` (0 regressions expected).
**CSS scoping invariant (iter 34+):** VS-EMBED selectors scoped к element-specific parent (не generic).
**Принцип `viz > dry text` (iter 8+):** сохраняется.

**Что НЕ сделано (потенциальные следующие задачи):**

- **KI#21 (Content Audit contradictions)** — 🟡 DOCUMENTED, fix plan ready в `docs/AUDIT_VERIFICATION.md` §4 для iter 35-38 (57 правок в 14 canon-файлах + 3 новые секции).
- **Glossary double-render inefficiency** (KI#24 minor) — `loadGlossaryContent()` вызывается дважды (init + content loader line 679), HTML rebuild происходит. Cache предотвращает re-fetch JSON, но не re-render. Можно оптимизировать в будущем iter если потребуется — добавить guard `if (glossaryContent.children.length > 0) return;` в начале `loadGlossaryContent()`.
- **Component extracts** (`visual-system/integration/component-extracts/E0X-visual.html`) — устаревшие relative to master HTML (имеют inline style="transition-delay:..." etc, KI#13 already migrated to `vs-ki13-*` classes в master HTML). Documentation drift, не критично — extracts не деплоятся. Можно синхронизировать в будущем iter если потребуется.
