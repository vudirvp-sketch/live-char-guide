# Live Character Guide — Project Status

> **Version:** 9.2.6 (canonical — `package.json` + `src/VERSION` + `data/character_schema.json`)
> **Date:** 2026-09-16
> **Iteration:** 159
> Full rules: `AGENTS.md` (operating law) · `AGENT_NAVIGATION.md` (system map) · `PLAN.md` (backlog)
> Iteration detail: `worklog.md` · History: `CHANGELOG.md` + git

---

## Current State

**iter 159 — v2 build slice — Appendix B (Model Capability Table) — the second appendix/Reference slice.** The pinned Next-step row 1, **not owner-gated** (the appendix slices = the pinned track continuation). Evidence — the appendix_model_table matrix table gains the F2 `presentation` column (**4 rows: all `open`** — the Reference-layer verdict holds; count script-verified) **+ the iter-145 НАБЛЮДЕНИЕ row added** (::03, METADATA — the block post-dated the iter-126 audit as DEC-20's first OBSERVATION application; the §1.1 document-order re-derivation; the audit total 500→**504** disclosed in the matrix header + Registry C §5 preamble — the script-examined count surfaced 3 pre-existing undisclosed post-audit additions (iter 139/142/147), now attributed) + Registry C §5.20; the ::01 stale marker inventory refreshed (canon `[Model: see Appendix B]` markers ×4 — Part 6 ×2 + Part 7A ×2 — + master `model-note` spans ×5, all WIRE-live); the ::04 (ex-::03) L-refs refreshed (the iter-145 insertion had shifted the block); the appendices verdict extended. Canon — **zero edits** (`appendix_model_table.md` byte-stable; F5-approximate verified — the Reference-layer shape is the target form; the OBSERVATION anchor intact — the sampling gate's check-8 probe PASS). Master — **two mirror completions** (fence #10, the iter-155/158 v9-slimming-gap precedent): the entire «Ключевые следствия для сборки карточки» block restored (h3 + 3 bullets + the `#p7a_core_directives` link on «см. §7A.2 ПРАВИЛО» — the v9 Phase-6 slimming had dropped it whole) + the Script Tax API-cell word order restored to the canon form; **the v7-era `part-resume` block disposed REMOVED_WITH_REASON** (the iter-158-recorded «B/C slices co-decide» sub-decision executed at B; the iter-47 P2-7 family precedent — the Parts were cleaned then, the appendices were out of that KI's scope; 5-point check + grep-verified zero tool deps; the stale bullet «Учитывать MODEL_NOTE…» directed readers at markers replaced by this very table). The P-B-4 observation package deferred (the A/C part-resume co-travelers, the heading-mismatch informational family, the front-matter staleness, the part_04 master-side span). Drift 142→141 (expected — the следствия mirror cleared the appendix_model_table plain-text entry; simulation pre-verified with the drift tool's own tokenizer, best-Jaccard 0.660; JSON pre/post diff-verified; the remaining entry = comment-noise, informational). Full battery green — sync 97/97 · parity ×7 (sampling PASS — the OBSERVATION framing intact) · tests 64/64 · baselines exact (english 18 / syntax 238) · build/validate/validate:master/version:check green · hash `2ab607d6` unchanged. Next: the Appendix C (Glossary) slice — the R01/R02 compression candidates ride it.

**iter 158 — v2 build slice — Appendix A (MBTI Reference) + the KI#70 WIRE (DEC-24 Q7).** Matrix — the appendix_mbti table gained the F2 column (6 rows: all `open`); canon byte-stable; master — three mirror completions + the stale v9-era transition disposed; **KI#70 EXECUTED and CLOSED** (the lazy-loader WIRE: appendices in the load list A→B→C + the «Приложения» TOC group; runtime-verified headless 93→96 sections, the `#appendix_mbti` TOC link scrolls, the `mbti-composer` initializes with zero page errors); drift 143→142. Detail: git `d053c03d`.

---

## Invariants

- **Version sync:** canonical 9.2.6 across `package.json` + `src/VERSION` + `data/character_schema.json` + build manifest (`pnpm run version:check`).
- **Themes:** Default (OLED/dark, no class) + Light (`body.theme-light`). No explicit dark class.
- **Mermaid:** absent by design (removed iter-113) — content diagrams = VS-EMBEDs only; no CDN script dependency.
- **CSS:** `src/assets/vs-styles.css` 3245 lines · `src/shell/styles.css` 7025 lines (both trimmed to in-use rules, iter 114/115).
- **Canon → master sync:** 97/97 PASS (`scripts/audit_canon_master_sync.py` MUST pass).
- **Voice Isolation:** linguistic voice = Examples/Greeting only; physical = Embodiment/Description.
- **OCEAN format:** compact `O:72 C:65 E:41 A:38 N:68` (§5.1 RULE).
- **Anchors format:** `<anchors>` XML canonical (src/master/ + parts/).
- **CORE_DIRECTIVES:** shorthand `{{CORE_DIRECTIVES — ...}}` accepted; #6 = Consequence Driven, #7 = Pre-Generation Filter.
- **SP Language rule:** layered — 12B <64K → English; ≥128K 12B–14B → either; 32B+/API → card language (§7A.2 RULE).
- **Identity name-language:** canonical form preserved across all card blocks — transliteration forbidden (§7A.1 RULE).
- **Script Tax:** non-Latin scripts ~1.5–2× tokens on 32K vocab; ≥128K → negligible (§7A.12 RULE).
- **Widgets:** 12 in `src/shell/widgets/`. `vs-e10-enneagram` consumes `data/enneagram.json` (fence #8, fixed iter 124).
- **VS-EMBED E06/E10 (iter 124):** E06 = two-column layout (`.ghost-rings-visual` + `.ghost-rings-annotations`), SVG geometry-only; E10 graph directions = `data/enneagram.json`, hexad complete, arrowheads encode direction (KI#73–76).
- **Editorial Policy (DEC-15, iter 122):** all `docs/canon/` + `src/master/` content edits — compress redundant presentation, never unique capability; 5-point functional-load check; UNCLEAR ≠ delete; success ≠ word count.
- **Language policy (DEC-16, iter 123):** two-layer — guide prose/labels/headings Russian (ordinary terminology translated); English only in executable prompt content (with adjacent Russian explanation), technical identifiers, proper names. `qa:english` baseline 18 (executable-content leaks by design; must not increase).
- **Glossary chain (DEC-17/18, iter 133):** `docs/canon/glossary_registry.md` = canonical term record (45 entries) → `data/glossary.json` **generated** by `scripts/generate_glossary.mjs` (first `pnpm run build` stage) → runtime panel / no-JS page. `glossary.json` never hand-edited; term changes go through the registry. `scripts/audit_glossary_parity.py` MUST PASS.
- **CORE DIRECTIVES single presentation (DEC-08/mig-2, iter 134):** §7A.2 = the only full definition; E08 = the one visual presentation (SHARED_REFERENCE); every other layer (§7A.1 template, §7A.13, Part 10) = `{{CORE_DIRECTIVES — …}}` shorthand; glossary entry = 1-sentence + link. `scripts/audit_core_directives_parity.py` MUST PASS.
- **Token budget single value owner (mig-4, iter 135):** §7A.12 = the only canonical budget table (SP 50/100/200 · Description 150/300/700 · Examples 40/60/80 per · Greeting 40/60/100 · Anchors 15/25/40 per); E01/E15 embeds = SHARED_REFERENCE presentations (Examples totals derived: per-example × count — derivation declared); E01 Lorebook row = declared estimate («Est.»). `scripts/audit_token_budget_parity.py` MUST PASS.
- **Enneagram single value owner (mig-5, iter 136):** §5.4 = the only canonical 9-type record (name/name_alt/fear/desire/LIE/flaw per type); `data/enneagram.json` = **GENERATED** (`scripts/generate_enneagram.mjs`, second stage of `pnpm run build` — never hand-edit; canonical value changes → `docs/canon/part_05.md` §5.4, machine-layer supplement changes → the generator); LIE lives once (`types[].lie_template`); E10 mini-cards = SHARED_REFERENCE. `scripts/audit_enneagram_parity.py` MUST PASS.
- **Diagnostics single canonical home (mig-3, iter 139):** §9.6 = the canonical owner of the E13 symptom→check→branch→AP/E mapping (sub-table «Три базовых симптома» + full `[VS: E13]` marker, DEC-20 TEXTUAL_CANONICAL); E13 = the one visual presentation / decision aid (re-pointed); §9.2↔§9.6 Debug chain wired (IMP-48 pair); §9.3/§9.11 = checklists. `scripts/audit_diagnostics_parity.py` MUST PASS.
- **Voice cluster single value owner (DEC-21, iter 142):** §3.2 = the canonical owner of the 6×3 influence table (values, `SHARED_REFERENCE`) + the «Хранилище ≠ Влияние» prose block (`TEXTUAL_CANONICAL` — E07 inset payload + the 2 widget claims ~10–20 сообщений / ~5 реплик); E07 = the one visual presentation (re-pointed); widget constants keep DEC-09 with the header note-deference line; row-1 label «Недавний чат» (canon+master+widget/export; embed labels stay); AN-12B framing = «пренебрежимо мало (~2%)» (Option A — table wins, no numeric change). `scripts/audit_voice_parity.py` MUST PASS.
- **Sampling values single owner (DEC-22, iter 145):** §7A.6 = the only canonical sampling table («Базовые параметры» 3 tiers: 32B+/API Temperature **0.7–1.0**, RepPen **1.05–1.10**; point recs via «Модель-специфичные»; AP-5 ≤ 1.10 and AP-7 PP = 0.0 unchanged); §7A.7 = capability checklist (params defer); E17 = the one visual presentation (`SHARED_REFERENCE`; 32B+ column = canonical values; 12B–32B middle tier = declared omission); E12/§9.x hints = model-qualified/deferred; glossary defers. `scripts/audit_sampling_parity.py` MUST PASS.
- **v2 architecture (DEC-24, iter 147):** the ratified spec is law — reader modes Learn/Build/Debug/Reference = **structural overlays** on the same canonical content (not four sites); F2 presentation vocabulary (`open/collapsible/reference-relocate/delete-candidate/canonical-link`) seeded on the matrix per Part at its build slice; F5 Part micro-template (Concept → Rule → Core example → ▸Why/▸Failure modes/▸Edge cases/▸Reference); F6 `<details class="interactive">` = the sanctioned disclosure mechanism; per-Part v2 build slices in guide order (**Parts 1–10 executed iters 147–157** — the Part-slice phase COMPLETE; **the appendix-slice stage OPEN**: Appendices A + the KI#70 WIRE executed iter 158 (map §5.19) and B iter 159 (map §5.20); appendices runtime-render since iter 158, 96 sections); v1 stays the frozen migration source until the switch (Q9: freeze/archive at switch). Part 1's slice set the shape: matrix F2 seeding → canon F5 restructure → master mirror → full battery (map §5.8; Parts 2–10 executed iters 148–157 per the same shape, maps §5.9–§5.18; Appendix A iter 158 + B iter 159 per the same shape, maps §5.19–§5.20).
- **Rule-strength markup (DEC-20, first application iter 145):** OBSERVATION ↔ «НАБЛЮДЕНИЕ» (base `.callout` — registered component); RULE ↔ ПРАВИЛО (`.callout.rule`), GUIDELINE ↔ РЕКОМЕНДАЦИЯ (`.callout.rec`); EXPERIMENTAL representation = first consuming slice (pending). Applied to Appendix B model-capability percentages.
- **Visual-markup ownership (DEC-19, iter 138):** master embed (L2) = the canonical visual markup; `visual-system/` L4/L5 = frozen design artifacts (archive = git history; E01/E08/E10/E15 prototype+extract copies REMOVED_WITH_REASON iter 138, E07 iter 142; hero disposed iter 140; remaining families ride the freeze at their slice time). Production `Source: elements/E##-*.html` provenance comments resolve to git history.
- **Rule-strength convention (DEC-20, iter 138):** RULE / GUIDELINE / OBSERVATION / EXPERIMENTAL ratified + UNKNOWN = temporary uncertainty marker only; applied incrementally per slice — no mass annotation; ПРАВИЛО (`.callout.rule`) ↔ RULE, РЕКОМЕНДАЦИЯ (`.callout.rec`) ↔ GUIDELINE; OBSERVATION/EXPERIMENTAL markup = first consuming slice (fence #7). The sampling cluster additionally waits on the KI#72 value decision.

Full rules and fences: `AGENTS.md` → Hard fences. Content semantics: `docs/canon/`.

---

## Known Issues

| KI# | Description | Status | Opened |
|-----|-------------|--------|--------|
| KI#70 | Appendices never runtime-loaded: `parts/manifest.json` has an `appendices` array (appendix_mbti / appendix_model_table / appendix_glossary) that `src/shell/lazy-loader.js` never reads — it iterates only `manifest.parts`. Appendix A (MBTI Reference + `mbti-composer` container), B (Model Capability Table), C (Glossary appendix) never render; the auto-injected TOC's `#appendix_*` links (built `part_01.html:414+`) are dead. Pre-existing across lazy-loader history (git `-S` finds no "appendices" consumer ever). Systematic, same symptom family as KI#69. **iter 147 (DEC-24 Q7): owner decision recorded — WIRE** (the recorded PLAN ki-70 recommendation); the implementation rides the v2 Reference/appendix slice (not before — per the ratified spec §7); the PLAN ki-70 decision gate is cleared, the wiring + its verification (runtime suite extension, 93→96 sections, `mbti-composer` init check) remain that slice's scope. **CLOSED iter-158 — the DEC-24 Q7 WIRE EXECUTED** (map §5.19 P-app-5): `src/shell/lazy-loader.js` `loadContent()` now appends `manifest.appendices` to the load list after `manifest.parts` (re-ordered by the `APPENDIX_GUIDE_ORDER` map to the guide lettering A→B→C — the manifest array is an alphabetical `build-unified.mjs` artifact) — the appendices inherit the KI#69 retry/placeholder machinery for free; `generateTOC()` gains the «Приложения» sidebar-TOC group (the ki-70 row's recorded TOC-grouping sub-decision — the auto-injected part-01 TOC's own convention; entry text = each appendix's own h2; DOM order = load order). Runtime-verified headless (the iter-119-suite extension, pre/post probes): 93→96 sections; appendix order A→B→C verified; all 3 appendix sections render; the `#appendix_mbti` TOC link target rendered and anchor navigation scrolls into view; the `mbti-composer` widget initializes on the live container with zero page errors / zero console errors; the sidebar TOC carries the 3 entries. | CLOSED iter-158 | 2026-09-14 (iter 119) |
| KI#77 | Canon-side wrong/stale cross-references (Phase B matrix findings; master resolves every affected link correctly — reader impact = canon navigability only). **(a)–(d) FIXED iter 127 (ed-8/R18):** (a) `part_04.md` L281 §7A.6 → §7A.2 (Consequence Driven home); (b)–(d) `appendix_character_map.md` — Omnis stale refs («Part 5 §5.2» false + «Part 8 §8.X AP-15» stale) → «Part 5 §5.1/§5.3 (концепты полюсов и перегрузки OCEAN)»; Выщербленный «Part 5 (эннеаграмма)» dropped (5w4 lives in §10.4, already listed); «Part 6 §6.X» → §6.5–§6.6. **(e) REMAINS OPEN:** character_map L37–38 budget ranges (~440–890 / ~1500–1800) vs Part 10 budgets (Walter ~1100, Omnis ~2150 outside) — R27 framing drift, needs the ed-5 re-frame decision (owner value decision). Evidence: `editorial_matrix.md` §18. | OPEN (e only) | 2026-09-14 (iter 126) |
| KI#79 | `scripts/audit_canon_master_drift.py` `P_TAG_RE = <p[^>]*>(.*?)</p>` also matches `<pre>` (and `<path>`/`<picture>` — any `p`-prefixed tag): a `<p>`-match can open on `<pre>` and run to the next literal `</p>`, absorbing code blocks + following paragraphs into one blob "paragraph". Surfaced iter 130: deleting the §3.2 Пояснение (last `<p>` before `</div>` after two `<pre>` blocks) shifted the absorption boundary — the master transition `<p>` was swallowed into the 499-char blob; informational actionable drift rose 159→160 with zero content drift (transition byte-identical canon↔master, sync 97/97 PASS). Informational-only tool (exit 0 always) — no reader impact; fix = regex tighten + actionable re-baseline (`PLAN.md` ki-79). | OPEN | 2026-09-14 (iter 130) |

> **KI lifecycle:** record → fix only if in scope → close as `CLOSED iter-<N>` → delete after 2+ iterations closed. Full rules: `AGENTS.md` → Bug → doc → fix.
> **iter-116 note (historical):** a suspected KI#66 ("workflow branch filters corrupted") was WITHDRAWN in the same iteration — byte-level verification proved the filters intact; the "corruption" was a terminal display artifact (pitfall #27).
> **iter-124/127/129/130/134/138 notes:** KI#67 (CLOSED iter-120) + KI#71 (CLOSED iter-121) deleted iter 124; KI#73–76 (all CLOSED iter-124) deleted iter 127; KI#78 (CLOSED iter 130) deleted iter 134 (closed >2 iterations); KI#79 opened iter 130 (drift-tool regex, informational); KI#80 (CLOSED iter 134, typo fold) deleted iter 138 (closed >2 iterations).
> **iter-145 note:** KI#81 (CLOSED iter-140, hero CSS) deleted iter 145 (closed >2 iterations — lifecycle rule); KI#72 CLOSED iter-145 (row kept per lifecycle); KI#82 (CLOSED iter-142) deletes at the next iteration touching STATUS.md.
> **iter-146 note:** KI#82 (CLOSED iter-142, content_map Notes) deleted iter 146 (closed >2 iterations — lifecycle rule); KI#72 (CLOSED iter-145) row kept per lifecycle.
> **iter-147 note:** KI#72 (CLOSED iter-145) row kept per lifecycle (2 iterations elapsed — 146/147; deletes at the next iteration touching STATUS). KI#70 stays OPEN: DEC-24 Q7 records the WIRE decision; the wiring itself rides the v2 Reference/appendix slice.
> **iter-148 note:** KI#72 (CLOSED iter-145) row deleted per lifecycle (2 iterations elapsed — 146/147). KI#70 stays OPEN: the WIRE implementation rides the v2 Reference/appendix slice per DEC-24 Q7.
> **iter-149 note:** KI#83 opened + CLOSED same iteration (the syntax-checker cross-line italic regex — forced by the slice's baseline gate; fix verified, baseline 245→238 recorded; the bold-pattern re-bounding deferred to the KI's verification pass). KI#70/77/79 unchanged (no lifecycle deletions due — no other CLOSED rows remain).
> **iter-150 note:** KI#83 (CLOSED iter-149) row kept per lifecycle (1 iteration elapsed — deletes after 2+). KI#70/77/79 unchanged. No new KI (the slice's only tooling-adjacent finding = the P4-5 observation package, recorded as a deferred registry row, not a defect).
> **iter-151 note:** KI#83 (CLOSED iter-149) row kept per lifecycle (2 iterations elapsed — 150/151; deletes at the next iteration touching STATUS). KI#70/77/79 unchanged. No new KI (the slice's findings = the P5-5 observation package + two stale matrix notes — registry rows, not defects; the pitfall-#27 display-artifact catch on the ::15 note disclosed in map §5.12 P5-2).
> **iter-152 note:** KI#83 (CLOSED iter-149) row deleted per lifecycle (3 iterations elapsed — 150/151/152). KI#70/77/79 unchanged. No new KI (the slice's findings = the P6-4 observation package + one stale matrix note — registry rows, not defects).
> **iter-153 note:** KI#70/77/79 unchanged (no lifecycle deletions due — no CLOSED rows remain). No new KI (the slice's findings = the P7A-4 observation package + seven stale matrix notes — registry rows, not defects; the §7A.7 family variances need a reconciliation owner call, recorded in map §5.14 P7A-4(b)).
> **iter-154 note:** KI#70/77/79 unchanged (no lifecycle deletions due — no CLOSED rows remain). No new KI (the slice's findings = the P7B-4 observation package + one stale matrix note — registry rows, not defects).
> **iter-155 note:** KI#70/77/79 unchanged (no lifecycle deletions due — no CLOSED rows remain). No new KI (the slice's findings = the P8-4 observation package + eleven stale matrix notes — registry rows, not defects; the P1-3 probe substring update = a disclosed in-slice tooling alignment per the iter-148 precedent, not a defect).
> **iter-156 note:** **KI#84 opened** (the master §9.6 Walter line value defect — see the row above; deferred with the two tool anchors + the §9.6 lock documented). KI#70/77/79 unchanged (no lifecycle deletions due). The sampling-audit check-7 re-point = a disclosed in-slice tooling alignment per the iter-148/155 precedent, not a defect; the P9-5 observation package + three stale matrix notes = registry rows, not defects.
> **iter-157 note:** **KI#84 CLOSED iter-157** (the owner-called canonical-audit reconciliation — the §9.6 Walter line deleted, §9.7 sole reference, both tool anchors moved together; map §5.18 P10-5; deletes after 2+ iterations per lifecycle). KI#70/77/79 unchanged (no lifecycle deletions due — no other CLOSED rows). No new KI (the slice's findings = the P10-4 observation package — registry rows, not defects; the two in-step tool-anchor re-points = disclosed alignments per the iter-148/155 precedent, not defects).
> **iter-158 note:** **KI#70 CLOSED iter-158** (the DEC-24 Q7 WIRE executed on the Appendix A slice — map §5.19 P-app-5; deletes after 2+ iterations per lifecycle). KI#84 (CLOSED iter-157) row kept per lifecycle (1 iteration elapsed — 158; deletes at the next iteration touching STATUS after 2+). KI#77/79 unchanged (no lifecycle deletions due). No new KI (the slice's findings = the P-app-4 observation package — registry rows, not defects; the TOC-grouping sub-decision + the guide-order map = disclosed in-slice WIRE decisions per the ki-70 row's recorded recommendation, not defects).
> **iter-159 note:** **KI#84 (CLOSED iter-157) row deleted per lifecycle** (2 iterations elapsed — 158/159). KI#70 (CLOSED iter-158) row kept per lifecycle (1 iteration elapsed — 159; deletes after 2+). KI#77/79 unchanged (no lifecycle deletions due). No new KI (the slice's findings = the P-B-4 observation package — registry rows, not defects; the part-resume disposal = the iter-158-recorded «B/C slices co-decide» sub-decision executed per the iter-47 P2-7 family precedent, disclosed in map §5.20 P-B-3).

---

## Next step (authoritative)

Re-pinned every iteration. **Owner-gated rows are not auto-candidates — the owner decides.**

**Active track since iter 131: v1 → v2 architecture migration** (owner directive, chat 2026-09-14). Migration order: `v1 (frozen) → semantic extraction → v2 architecture → v2 build → parity audit → canonical audit → reader-path audit → switch`. Reader modes: **Learn → Build → Debug → Reference**. Semantic extraction **COMPLETE (7/7)**; the v2 architecture phase **COMPLETE** (spec P-a iter 146; **RATIFIED — DEC-24 iter 147**). The **v2 build phase is OPEN**: the Part-slice stage **COMPLETE** (Parts 1–10 executed iters 147–157 — maps §5.8–§5.18 the pattern; KI#84's owner-called canonical-audit reconciliation executed iter 157); the appendix-slice stage **OPEN** (Appendix A + the KI#70 WIRE executed iter 158 — map §5.19; Appendix B executed iter 159 — map §5.20; C/D follow).

1. **v2 build slice — Appendix C (Glossary) — the third appendix/Reference slice:** the next slice in guide order after Appendix B (`docs/canon/appendix_glossary.md` + `src/master/appendix_glossary.html`; 1 section / 26 matrix rows). Slice shape = the iter-147…159 precedent (spec §7, maps §5.8–§5.20): (a) evidence — seed the appendix_glossary matrix rows with F2 presentation dispositions (+ Registry C §5.21) + the stale-note-refresh step (the per-row «`§X.X` vague» notes predate the iter-127 R18 repair); (b) canon — restructure per the F5 micro-template where honestly needed (Editorial Policy 5-point check per edit; section IDs preserved); (c) master — mirror + apply F2. Notes: **the R01/R02 compression candidates ride this slice** (the glossary «B — Behavioral Anchor» ПРАВИЛО fold to 1 sentence + ref §1.4 per the glossary's own pattern; the «C — CORE DIRECTIVES» full annotated copy → definition + 7-name index + fixed ref §7A.2 — the matrix COMPRESS/ed-2 verdicts; DEC-17c = the v2 Reference-layer representation; the KI#70-wire dependency recorded in the matrix §14 R02 note is resolved since iter 158); the KI#70 WIRE already loads it at runtime (iter 158 — no infrastructure work remains); the v7-era `part-resume` block follows the iter-159 B precedent (map §5.20 P-B-3). **Not owner-gated** (the appendix slices are the pinned track continuation).
2. **After the appendix slices:** **parity audit** → **canonical audit** (the P4-5 + P5-5 + P6-4 + P7A-4 + P7B-4 + P8-4 + P9-5 + P10-4 + **P-app-4 + P-B-4** master-only enrichment observations ride this stage; KI#84's reconciliation EXECUTED iter 157 per the owner's call — map §5.18 P10-5) → **reader-path audit** (per mode, the iter-139 Debug precedent) → **switch** (Q8 criteria: stages 2–4 green in two consecutive full-battery runs + no OPEN KI in the affected families + owner call; Q9: v1 freezes/archives at switch).
3. **Superseded (per owner directive — do not auto-resume):** `Fork D 2/3` (sampling widget), `Fork D 3/3`, `dupes-2` and equivalent sequential cleanup batches. `ki-70` standalone wiring — superseded by DEC-24 Q7 (WIRE decided; rides the appendix slice). `ki-79`, `cache-bust-1`, `vs-e09-hex`, obs-3/4/5/6: deferred unless a migration slice requires them.

A new agent: run the preflight in `AGENTS.md`, confirm this Next step against the worktree (`git log` vs iteration above), then start. iter-159 (Appendix B v2 build slice) is complete and verified — full battery green (sync 97/97, drift 142→141 — the следствия mirror completion cleared the appendix_model_table plain-text entry, JSON pre/post-verified; parity ×7 — sampling PASS, the OBSERVATION framing intact; tests 64/64, build/validate/validate:master/version:check green, baselines exact english 18 / syntax 238, hash `2ab607d6` unchanged; runtime unchanged — 96 sections, the WIRE live since iter 158). Seven cluster audits MUST PASS: `python3 scripts/audit_glossary_parity.py`, `python3 scripts/audit_core_directives_parity.py`, `python3 scripts/audit_token_budget_parity.py`, `python3 scripts/audit_enneagram_parity.py`, `python3 scripts/audit_diagnostics_parity.py`, `python3 scripts/audit_voice_parity.py`, `python3 scripts/audit_sampling_parity.py` (all wired into `AGENTS.md` canon-audits). `data/glossary.json` is **generated** (`pnpm run build:glossary`) and `data/enneagram.json` is **generated** (`pnpm run build:enneagram` / inside `pnpm run build`) — never hand-edit; term changes go through `docs/canon/glossary_registry.md`, enneagram canonical values through `docs/canon/part_05.md` §5.4, the enneagram machine-layer supplement through `scripts/generate_enneagram.mjs`. v1 remains the frozen migration source — canon/master content edits require an explicit v2-build-slice scope (the Part 1–10 slices iters 147–157 + KI#84's owner-called reconciliation + the Appendix A slice carrying the KI#70 WIRE iter 158 + the Appendix B slice iter 159; the Appendix C slice next).
